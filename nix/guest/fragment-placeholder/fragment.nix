# The flake's `fragment` input points here so flake.lock is valid; hostd
# overrides it per Build with the revision's directory
# (docs/interfaces/nix-build-contract.md). An empty fragment is the base.
{ }
