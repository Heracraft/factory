test fixture keys are throwaway values generated for the VM tests; nothing here is real
The bootstrap_ed25519 pair is the host-services test operator key for `repose.host.bootstrap.keyUntilHostCA` (DECISIONS I-177); it is accepted only by that test VM.
