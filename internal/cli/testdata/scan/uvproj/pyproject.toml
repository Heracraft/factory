[project]
name = "svc"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "gunicorn",
    "celery[redis]>=5.4",
]

[project.scripts]
svc = "svc.cli:main"

[dependency-groups]
dev = ["ruff>=0.6", "pytest", "mypy"]
