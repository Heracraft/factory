{
	"name": "@play/wasm",
	"scripts": {
		"build:wasm": "cd core && GOOS=js GOARCH=wasm tinygo build -o ../main.wasm",
		"build": "tsc",
		"dev:wasm": "air",
		"dev:ts": "tsc --watch",
		"dev:serve": "node scripts/serve.js",
		"dev": "turbo run dev:wasm dev:ts dev:serve",
		"prepublishOnly": "pnpm run build:wasm && pnpm run build"
	},
	"devDependencies": {
		"typescript": "^5.6.0"
	}
}
