# Workstream status

One line per claim. Newest at the bottom. Format:
`YYYY-MM-DD | <workstream> | <who> | claimed|progress|done|blocked | <note>`

2026-09-17 | docs | interview session | done | design, decisions, interfaces, workstreams written
2026-09-17 | scaffold | interview session | done | go.mod, cmd stubs, proto contracts, buf config, nix flake skeleton (flake check passes), infra skeleton, justfile, AGENTS.md
2026-09-17 | 00-benchmark | deferred | | owner skipped it (DECISIONS I-12); first M1 host records timings instead
2026-09-19 | 01-host-nixos | ws/01-host-nixos agent (Fable 5.1) | claimed | nix/hosts/: disko, kernel, network, nftables, storage, virt, hostd unit + stub, gc, observability, registration, ssh, hardening, azure, VM tests; flake check and host toplevel build locally; real-host items marked
2026-09-19 | 01-host-nixos | ws/01-host-nixos agent (Fable 5.1) | done (local) | nix/hosts complete: nixosConfigurations.host and host-bench build, nix flake check passes with 3 VM tests (network isolation, thin pool, services/registration/ssh audit); host-conventions.md rewritten, DECISIONS I-18, runbook entries. Not done: every real-host item (nixos-anywhere, lsblk on Azure, Grafana view, ss on the Azure NIC) and CI (no CI exists yet); buf lint fails on the untouched proto scaffold. hostd is the stub until 03 merges packages.hostd.
2026-09-19 | 11-infra-opentofu | ws/011-infra-opentofu-host session | claimed | infra/bootstrap, infra/azure (network, host, edge, coolify, storage, keyvault, dns), infra/r2, Makefile targets, README; tofu validate + plan clean
2026-09-19 | 11-infra-opentofu | ws/011-infra-opentofu-host session | progress | Written and verified without applying. Done: infra/bootstrap; infra/azure/modules (environment, network, host, edge, coolify, storage, keyvault, nixos_anywhere); infra/dns as a module so records are in the same apply as the addresses; infra/azure/{prod,staging} roots on the real backend; infra/r2; Makefile (plan/apply/validate/policy/check/bootstrap/force-unlock); tfsec policies proven against a fixture; CI workflow; README with bootstrap, add host, rotate token, drain/destroy, force-unlock and a cost table re-derived from the Azure price API on 2026-09-19 (~$1,040/mo pre-launch, over the $1,000 budget alert). `tofu plan` on prod is clean: 31 to add, 0 to change, 0 to destroy, no errors or warnings; 39 with one host. Five guards triggered deliberately, each with its documented message. Docs: DECISIONS I-19..I-22, host-conventions join-token row, workstream doc, three runbook entries. NOT done, all of it needing an apply the owner authorises: host registers, /dev/kvm on a real host, IMDS from a host, the prevent_destroy replacement attempt against real state, the Blob lifecycle deletion, Key Vault wrap/unwrap and the denied get, R2 backup and restore rehearsal, DNS resolution, and somebody following the README end to end. Also waiting on a human: blob soft delete on reposetfstate3912 (versioning is on, soft delete is off), the api Entra app registration and the R2 token (I-20), a CLOUDFLARE_API_TOKEN before manage_dns can be true.
2026-09-19 | 11-infra-opentofu | ws/011-infra-opentofu-host session | done | Owner reviewed and set the shape: nothing applied from this branch; the apply happens from main after 01 merges, so the host installs from the merged flake. Changes since the progress line: coolify_count defaults to 0 so the control-plane VM waits for wave 3 (I-24); the installer reaches hosts through the edge, never a temporary public IP, documented and decided (I-24); the join token comes from `hostdev init` (I-17), not repose-admin, so workstream 05 is not on the path to a registered host; edge_operator_ssh_port is a root variable and must be 22 for the first apply until 06 moves the edge's sshd. Blob soft delete enabled on reposetfstate3912 (30 days, blobs and containers; versioning was already on) - the only change made to real infrastructure. `tofu plan` on prod: 28 to add, 0 to change, 0 to destroy, no errors or warnings; make check clean. Branch is ready to merge.
2026-09-19 | 03-hostd | ws/03-hostd-hostdev agent (Fable 5.1) | claimed | cmd/hostd, internal/hostd/*, cmd/hostdev (I-17), internal/fakes/{hostd,guestd}, buf-generated proto; host-level checklist items need a real host and are marked
2026-09-19 | 03-hostd | ws/03-hostd-hostdev agent (Fable 5.1) | progress | done without a host: cmd/hostd (register, mTLS stream with reconnect/replay/sample buffering, every command against LVM/nft/tc/systemd-run/CH API/virtiofsd/nix behind interfaces with fakes, bbolt state, reconcile, GC roots, samples, console capture, metrics, control socket, operator subcommands), cmd/hostdev (I-17), internal/fakes/{hostd,guestd}, internal/vsockrpc, proto I-26 fields, nix packages hostd/hostdev, decisions I-26..I-20, nix-build-contract.md, runbook rows. NOT done (needs a real Intel host): every host-level checklist item in 03-hostd.md §9 (real CreateGuest, isolation curls, blob restore diff, freeze p99, nix-collect-garbage, console rotation in Loki, chaos kill -9 mid-command on a host, RESEARCH.md timings). Fluent Bit config is 10's; the api-side audit_log for `hostd audit-login` is 05's.
2026-09-19 | 04-guestd | opus-5 session (ws/04-guestd) | claimed | vsock server, all requests, notifies, hook socket, repose-hook, fakes, VM test
2026-09-19 | 04-guestd | opus-5 session (ws/04-guestd) | done | cmd/guestd (+ `call` client), cmd/repose-hook, internal/vsockrpc, internal/guestd/*, internal/fakes/guestd, nix packages.guestd and the NixOS VM test. `nix flake check ./nix` green, `go test -race` green, lint clean. Interfaces: vsock-guestd.md; DECISIONS I-29..I-21. NOT done: the real-guest item (needs a host and hostd, workstream 03); `mkGuest`/guest base installation of the unit is 02's.
2026-09-19 | 02-guest-base | claude session (ws/guest-base worktree) | claimed | nix/guest/base modules, overlay wrappers and hooks, mkGuestRunner, VM tests, retire packages/core/flake.nix
2026-09-19 | 02-guest-base | claude session (ws/guest-base worktree) | done | nix/guest/base (17 modules), nix/overlay/agents wrappers+hooks+MCP, mkGuestRunner (bin/run with run-time guest args, I-34), VM tests guest-base/guest-docker/guest-desktop + closure check pass, runner booted under Cloud Hypervisor on the dev box with SSH-cert login, docker, desktop; packages/core retired. Not done: hooks.sock end-to-end and hostd AgentEvent (needs 04 guestd), IMDS/guest-to-guest blocking (host nftables, 01/03), CI wiring; gemini-cli is marked for removal in nixpkgs (12 decides).
2026-09-19 | merge wave 1 | interview session | done | 01, 11, 03, 04, 02 merged into main (2eed4f7): flake check, go build/vet/test, nix builds of guestd/hostd/hostdev/repose-hook all pass; decisions renumbered I-18..I-38; 03/04 vsockrpc duplication kept as I-37; ci.yml added; prod plan clean (28 resources, hosts=[]). Apply blocked for the owner to run (permission), see PROMPTS.md "M1 bring-up".
2026-09-19 | m1-integration | ws/m1-integration session (Fable 5.1) | claimed | M1 bring-up per PROMPTS.md: hostdev on the edge, host-01 registration, real-host checklist items of 01/02/03/04, timings into RESEARCH.md
2026-09-20 | m1-integration | ws/m1-integration session (Fable 5.1) | blocked | First apply failed (SkuNotAvailable; every Dsv5/Dsv6 size is NotAvailableForSubscription in eastus, RESEARCH §2a; owner moved main to v7, I-39). Done on the branch: I-40 (hostd snapshot target and api CA options, hostdev-tolerant host-net, nixosConfigurations.host-01 with the edge address, hostdev CA, bootstrap key and Blob target, host_flake_attrs, edge 443), I-41 (data disk resolved at install time), merge of main; hostdev init done on the dev box, token in the worktree's local tfvars; host-01/edge toplevels build, host VM tests pass, tofu validate clean, prod plan from the worktree: 15 to add, 1 to change, 0 to destroy. NOT done: everything needing a real host or edge (hostdev on the edge, registration, every real-host checklist item of 01/02/03/04, timings in RESEARCH.md); waits on the owner's apply.
2026-09-20 | 10-observability | ws/10-observability session (Opus 5) | claimed | internal/obs (logger, metrics, tracer, event and metric names, naming/redaction tests), migration of every binary onto it, nix/hosts/fluent-bit.nix, ops/ (prometheus scrape, 7 Grafana dashboards, 11 alert rules with promtool tests, Loki retention, local dev stack, partition drops); real-host items marked
2026-09-20 | 10-observability | ws/10-observability session (Opus 5) | done (local) | internal/obs (logger with component and redaction enforced by the handler, registry that refuses a metric outside repose_ or a label outside the low-cardinality list, OTel with a no-op exporter when OTEL_EXPORTER_OTLP_ENDPOINT is unset, HTTP middleware, gRPC options, event and metric name registries), internal/obs/obslint run by a test over cmd+internal, every existing binary migrated (hostd with --log-level, guestd, hostdev, hostd metrics registry), ops/ (7 dashboards as 56 panels from a generator, 13 alert rules with 17 promtool cases, Prometheus scrape config and WireGuard peer, Loki and Prometheus retention, Alertmanager route, partition SQL, local Loki/Prometheus/Grafana/Postgres stack, seedmetrics, check.sh, pgcheck.sh), nix/hosts/fluent-bit.nix with its own metrics port. Fixed in merged code: repose_host_guestd_unreachable{guest_id} (forbidden label), argv in hostd's audited-exec log line, guestd thaw/freeze_timeout emitted through a variable so no grep found them, SetupTracing failing for every caller on a semconv version conflict. Decisions I-42..I-47; interfaces db-schema (meter_samples.guestd_ok) and host-conventions (Fluent Bit metrics port on wg0). NOT done: api, gateway and cli events and metrics (13 + 5 events and both families are defined in obs but their components are workstreams 05, 06 and 07 - they must construct their loggers and registries from obs, and 05 must schedule repose_partitions_maintain() and set repose_api_egress_alert_projects); every real-host item (Fluent Bit to the real Loki, Prometheus scraping over WireGuard, panels with real data, screenshots, an alert firing for real); the api-side partition migration (05 owns internal/db/migrations, ops/dev/schema-fixture.sql is the stand-in).
2026-09-20 | 10-observability | ws/10-observability session (Opus 5) | progress | Five bugs in merged code, found by building the enforcement rather than by reading: repose_host_guestd_unreachable carried the guest_id label §5 forbids; hostd logged an operator's exec argv; guestd emitted thaw and freeze_timeout through a variable so no grep found them; obs.SetupTracing failed for every caller on a semconv schema conflict, which means tracing had never been switched on; and repose-hook read REPOSE_AGENT while the wrappers export REPOSE_HOOK_AGENT, so every agent hook in every guest was silent (I-48, fixing 02 and 04). Also repaired two host-services VM assertions that the hostd stub left behind at merge wave 1 (stub_start, and a snapshot-all that cannot work without a control socket) - `nix flake check` has been red on main since that merge. It still is, on one assertion I did not touch: host-services expects repose-register to succeed against an api the VM does not have, which needs hostdev or a fake api in the test node and belongs to 01 and 03.
2026-09-20 | 05-control-plane-api | ws/05-control-plane-api agent (Fable 5.1) | claimed | cmd/api, cmd/repose-admin, internal/api/* (http, auth, grpc, scheduler, ca, secrets, config, ops, snapshots, events, notify outbox, meter rollup, internalroutes, ratelimit), internal/db migrations, internal/fakes/{api,kv,blob}, internal/ca/testca, Dockerfile, OTel; integration tests against a real Postgres; staging/Coolify items marked as needing a deploy
2026-09-20 | 05-control-plane-api | ws/05-control-plane-api agent (Fable 5.1) | done (local) | cmd/api (API_MODE http|grpc|all, migrations, graceful drain), cmd/repose-admin (full I-9 surface), internal/api/{http,auth,hostmgr,ops,scheduler,ca,secrets,config,buildlog,events,meter,notify,snapshots,basebump,ratelimit,metrics,app}, internal/db (2 migrations, up/down, partitions, temp-Postgres test helper), internal/{obs,otel,billing,nixmenu,admin,ca/sshca,ca/testca,api/pki}, internal/fakes/{api,kv,blob,logto}, cmd/api/Dockerfile, ops/coolify/api.env.example, CI with Postgres and the down-1 job, nix packages api/repose-admin (vendorHash updated). All 41 api.md routes registered (contract test); every op end to end against the fake hostd including api restart and stream drop; 100-parallel-creates placement test; secrets round-trip and rewrap; rollup synthetic day = 153 cents; go test -race, golangci-lint, buf lint, nix flake check, nix build api/repose-admin/hostd all pass. Docs: DECISIONS I-42, db-schema.md as built, api.md (SSE and sessions shapes), ssh-gateway.md host cert principals, RUNBOOK api entries. NOT done (needs a deploy): real first sign-in on staging, real Key Vault rewrap, real build on the M1 host, curl against /internal on staging, Coolify rolling deploy and api-grpc restart during a build, hosts add registering a real host, healthz with the DB paused on staging, Stripe test-mode push (09). Not built: the second-replica command forwarding hop (05 §5.1), `repose-admin login`.
2026-09-20 | 12-nix-config-pipeline | ws/12-nix-config-pipeline agent (Fable 5.1) | claimed | fragment contract in nix/guest/{fragment,compose}.nix + flake guestSystem/fragment input/checks.fragment-examples, restricted eval and limits in internal/hostd/nixbuild (nixbuild user, scope, exact five canonical messages, real-nix test corpus), internal/menu catalog+renderer+allowlist lint, internal/basebump, nix/overlay/agents from versions.json + scripts/bump-agents.sh + workflow, docs/features/config.md + config-examples, runbook, RESEARCH timings
2026-09-20 | 12-nix-config-pipeline | ws/12-nix-config-pipeline agent (Fable 5.1) | done (local) | Done without a host: fragment contract as nix/guest/contract.nix (repose.overlays pre-pass, repose.system through nix/guest/system-allowlist.json, errors attributed to fragment.nix), nix/guest/compose.nix, flake inputs.fragment + guestSystem + lib.composeGuest + checks.fragment-examples/fragment-contract, three docs/features/config-examples that build; internal/hostd/nixbuild extended in place (git+file flakeref, allowed-uris from flake.lock, nixbuild user via setpriv in a scope with RuntimeMaxSec, --show-trace, the doc's exact messages, nix log tail, KernelChanged, ten real fixtures, real-Nix corpus in realnix_test.go incl. the five canonical cases with timeout 5 s and cap 100 MB, GC root, kernel_changed); nix/hosts nixbuild user, builds dir 0711, baseRepo/buildUser/overlayCache passed to hostd; internal/menu (21-entry catalog, validate, render with round-trip header, allowlist lint, full-catalog eval on the base); internal/basebump (plan/run/summarize/rollback/status line, three-project test); nix/overlay/agents from upstream release binaries pinned in versions.json (all five print their version), scripts/bump-agents.sh + bump-agents.yml, ci.yml runs the corpus and pushes to Cachix when the token exists; DECISIONS I-43..I-46; nix-build-contract.md rewritten; features/config.md 'Writing a fragment'; api.md /catalog; runbook x5; RESEARCH §6 update and §11 timings (eval 3.6-4.1 s, typical build 25 s on the dev box). nix flake check --no-build, go test ./..., just lint, done-check all clean. NOT done, needs a host or a later workstream: systemctl show of the nixbuild scope during a real build, the real 20 GB+ closure_too_large on a host, nix-collect-garbage on a host, the CLI runs of the five cases (07), the menu->fragment->edit->takeover flow through the api and the base bump with repose status (05/07 wire internal/menu and internal/basebump), a merged bump-agents PR and the populated Cachix cache (owner creates the cache and the CACHIX_AUTH_TOKEN secret, ops/AZURE-SETUP.md step 16), host timings in RESEARCH.
2026-09-20 | 14-security | ws/14-security agent (Fable 5.1) | claimed | review of isolation claims in SECURITY.md against nix/ and Go as merged; test/isolation suite from CHECKLIST.md; audit, secrets and operator-access review; privacy policy and terms in apps/web/src/content/legal/; incident runbook entry; findings list with severity and file:line; fixes only for clear bugs
2026-09-20 | 14-security | ws/14-security agent (Fable 5.1) | review 04-guestd | secrets handling reviewed, no findings: values 0400 dev on tmpfs in a 0700 dir, secrets.env shell-quoted, reserved sshd names root-owned under /run/repose, validation before the first write, counts only in logs; L-5 (name regex wider than api.md) for 05 to enforce. Sample boundary: strace tests hold; proc.go reads stat and status only.
2026-09-20 | 14-security | ws/14-security agent (Fable 5.1) | review 03-hostd | two fixes on this branch (H-1 tap admission per host-conventions, H-3 virtiofsd namespace sandbox); open: H-2 Cloud Hypervisor as root, M-1 no Host CA in RegisterResponse, M-5 second wg0.conf writer, M-8 unvalidated user_id/project_id/blob_path, L-4, L-7, L-9. Secrets: memory only, never bbolt, guest.json or ch.args; delivered after Ready. hostdev: M-4 plaintext secrets and host keys in state.json and on argv.
2026-09-20 | 14-security | ws/14-security agent (Fable 5.1) | review 01/02/11 | 01: L-1 global ICMP limit, L-10 ip_nonlocal_bind; bridge table, store export with .links masked, thin pool --zero y, IMDS and private ranges all verified. 02: M-6 serial getty on the captured console, L-6 dev in kvm; sshd principals, CA-only auth, no authorized_keys verified. 11: M-3 one Blob identity fleet-wide, M-2 edge sshd on NixOS defaults behind an internet-open 22; hosts NSG empty, join token by file over SSH, Key Vault wrap/unwrap only verified.
2026-09-20 | 14-security | ws/14-security agent (Fable 5.1) | review 05, 07 | not started; the secrets review comments the checklist asks for wait on those workstreams. Checklist for them: docs/SECURITY.md "Reviewing a workstream".
2026-09-20 | 14-security | ws/14-security agent (Fable 5.1) | done (review complete; host runs pending) | Done: docs/security/review-2026-09-20.md (21 findings with severity and file:line; H-1 and H-3 fixed on the branch: hostd tap admission per host-conventions with MAC in the Net interface and golden, virtiofsd --sandbox namespace, DECISIONS I-48); SECURITY.md revised; test/isolation (16 host tests keyed on REPOSE_ISOLATION_* env, 4 CI policy tests passing); privacy.md and terms.md with the two passages, pinned by test; runbook incident procedure; secrets review lines for 01/02/03/04/11. NOT done: every host-level checklist item (needs host-01 with two guests of two users: the 13 boundary rows, the 10 percent neighbour numbers, operator cert login, audit_log rows, incident commands on staging); rows that wait on 05/06/07 stubs (gateway, revocation, 404, secrets list, audit_log, repose-admin audit, secrets review of 05/07); the open findings (H-2 CH as root, M-1 Host CA in RegisterResponse, M-2 edge sshd, M-3 Blob identity, M-4 hostdev secrets, M-5, M-6, M-7, M-8) belong to their owners.
2026-09-20 | 06-gateway-edge | ws/06-gateway-edge agent (Fable 5.1) | claimed | cmd/gateway (serve, wgsync), internal/gateway/* (server, relay, revocation and CA caches, route and gateway-cert caches, session reporting, metrics, wgsync, hook ingest, preview stub), nix/edge (wg0 hub, nftables, gateway and wgsync units, hook ingest, operator sshd on 2222, Fluent Bit, node_exporter, hardening, VM test), fakes/api CA-backed internal routes, runbook rows; staging-edge items marked as needing a deploy
2026-09-20 | 11-infra-opentofu (control plane) | ws/11-infra-control-plane session (Opus 5) | claimed | Owner-scoped remainder of 11 now that wave one is merged and the edge and host-01 exist in prod: turn on the control-plane VM (coolify_count=1, cloud-init, NSG, DNS), wire Coolify's Postgres backup to infra/r2, make manage_dns usable the moment a CLOUDFLARE_API_TOKEN exists (false until then), and close the 11 checklist items a live apply can now prove. Nothing is applied from this branch; the owner applies from main.
2026-09-20 | 07-cli | ws/07-cli agent (Sonnet 5) | claimed | cmd/repose + internal/cli: every command in 07-cli.md §5.1, Logto PKCE/device login, project resolution, cert issue/refresh + ssh config, run sequence (sync, credential copy, tmux send), open/desktop, status, SSE build log rendering, install.sh + goreleaser, shell completion. Testing against internal/fakes/api and a local sshd; no real Logto or real guest available here so those rows are marked.
2026-09-20 | 07-cli | ws/07-cli agent (Sonnet 5) | done (local) | cmd/repose + internal/cli, every command and flag in 07-cli.md §5.1 (`repose --help` tree diffed against it) plus I-8's events/notify. Login: PKCE loopback and device code against Logto's real `/oidc/...` paths (I-73; matches internal/api/auth's own two call sites), tested against a hand-written fake OIDC server (internal/fakes/logto is server-side JWT verification, not a user login flow). Cert: ensureCert reuse/refresh/reissue with a clock, exactly-one-Include-line golden test. Run sequence: create-if-new, start-and-wait (streaming the build log if one appears), sync (dirty-tree refusal exit 6, --stash-remote, --discard-remote, unpushed-commit prompt-and-push, git diff --binary + git apply --index for tracked changes, tar for untracked, 100 MB skip), credential sync (exactly the 4 rows, never Claude/Gemini/SSH keys), tmux prompt send (window naming incl. -2, pane-idle wait, Claude-not-logged-in attaches instead), attach (process-replacing exec). open (port forward + free-port fallback, --desktop), status/projects/secrets/config (apply/edit with the Nix error block)/snapshots/logs/events/notify/destroy/resize all round-trip against internal/fakes/api. stop/start/resize/destroy retry DECISIONS I-70's op-conflict 409 for 10s. install.sh (real download+checksum+PATH-collision test against a local server), .goreleaser.yaml (4 targets built and run via `nix shell nixpkgs#goreleaser`), nix/packages.nix gained the `repose` package (`nix build ./nix#repose`, `nix flake check --no-build ./nix` both pass), bash/zsh/fish completions load (verified via `nix shell nixpkgs#zsh nixpkgs#fish`). CI gained cli-goreleaser/cli-nix-run/cli-install/cli-completions jobs (not yet run for real, no push happened from here). `go build/vet ./...`, `golangci-lint run ./...` (0 issues), `buf lint`, `just done-check` all clean; `internal/testguest` (an in-process SSH server, not the doc's Docker fixture — DECISIONS I-73, same trade 06 made) backs the sync/credential/tmux/run integration tests, ~90 test functions total. Merged main twice (last at 1f4d858) for I-70 and the wave-3 vendor hash. NOT done, all needing things this dev box does not have: real Logto (login recording/transcript, macOS keychain absence-from-disk check), a real gateway+guest (`ssh <slug>.repose` from a plain terminal, `open`/`attach` transcripts, `ps` showing no repose parent, the gateway-rejects-certificate retry-once path — 06-gateway-edge is unbuilt as of this commit), a real host for the 10s/60s boot timings, and an actual CI run of the four new jobs. `repose status --json | jq .` verified locally, not yet in CI. `-v`/`--verbose` is a reserved flag with nothing wired to it yet (nothing currently logs at all, so nothing leaks, but it does nothing either).
2026-09-20 | 08-dashboard | ws/08-dashboard agent (Sonnet 5) | claimed | apps/web: Logto SPA login, projects list/detail, config (menu+Nix), secrets, billing, settings, account, landing, Dockerfile+healthz, Playwright against internal/fakes/api plus a new cmd/fakeapi and test/fake-logto fixture
2026-09-20 | 08-dashboard | ws/08-dashboard agent (Sonnet 5) | done (local) | Note: built on branch ws/08-dashboard-b (a second worktree/branch), not ws/08-dashboard — a stray concurrent process was writing to the original worktree; the owner's conductor session confirmed ws/08-dashboard-b is the one to merge and will retire the original. Merged main (f48b946) in; no conflicts. Every 5.2 route built: Logto SPA auth (`auth.svelte.ts`, PKCE, $env/dynamic/public, ssr=false); Tailwind v4 design system in `routes/layout.css` per DESIGN-LANGUAGE.md (chip/segmented/card-radio replaced with checkbox rows, a switch, radio rows, and a searchable check-list); a hand-written typed api client (`lib/api/client.ts`) and TS types mirroring api.md; projects list/detail with start/stop/destroy(confirm-typed slug)/resize/snapshots(restore, restore-as-new)/events; config page with a real CodeMirror 6 Nix editor (`@replit/codemirror-lang-nix`), Menu tab against `GET /catalog` (Services pulled out by `kind`, search, per-item options), SSE build-log streaming, fragment-line error highlighting (parsed client-side from the `fragment.nix:L:C` in the error text — no interface change needed), hold-base-updates, revisions/re-apply; secrets (add via text/file, 64 KB and name-regex client validation, values never re-rendered); billing (Stripe PaymentElement in setup mode, degrades to "not enabled yet" on `billing_disabled`, a stacked usage-by-class chart using the dataviz skill's validated 3-slot categorical palette); settings, account (confirm-typed handle), landing (install command, DESIGN §2 terminal example, PRICING.md table), /terms and /privacy rendering `content/legal/*.md` with a draft banner from frontmatter. Dockerfile: alpine + the stripped node binary only (build/ is a fully self-contained Rollup bundle — verified zero non-`node:*` imports — so no node_modules at all in the runtime image), non-root `repose` user, wget HEALTHCHECK, 179 MB built and run locally. Two real bugs found by actually running this in a browser against the fake, fixed at the source: `internal/api/http` and `internal/fakes/api` had no CORS headers at all, so no browser fetch from a different origin than the api's could ever have worked in production (DECISIONS I-79); `pollWhileVisible` didn't back off on api failure as 08-dashboard.md §6 requires, only on tab visibility (fixed in `poll.ts`). `internal/fakes/api` also gained `kind`/`options` on `CatalogItem` (matches api.md/I-44, which the fake predated) and a `repose-force-eval-error` fragment marker so the error-highlighting path is testable without real Nix (I-62); a new `cmd/fakeapi` (wraps the fake on a random port, plus an admin HTTP listener driving `Fail`/`FailNext`/`Unfail` for Playwright) and `cmd/fake-logto` + `test/fake-logto` (a real authorization-code-with-PKCE OIDC provider, JWKS-signed tokens, refresh grant; `go test ./test/fake-logto/...` covers the flow, a wrong PKCE verifier, and refresh). Evidence: `go build/vet ./...` clean; `pnpm exec svelte-check` and `pnpm exec eslint .` clean; `pnpm exec vitest run` 22/22 (api client error mapping, menu selection state, cost projection math, remote-URL normalisation — the doc's four named targets); `pnpm exec playwright test` 28/28 against the real fixtures in an actual browser (Nix-provided Chromium — this sandbox's own Playwright-downloaded Chromium is glibc-incompatible here; unaffected on a normal runner), covering sign-in/callback/sign-out, every 5.2 route, start/stop/destroy/resize with op progress, menu search+apply+generated-fragment, the Nix editor's canned eval error with line highlighting, hold-base-updates round-trip, secrets add/list/delete with a DOM-content assertion that the value never appears, settings+notify-test, account deletion, and three of §6's failure rows (payment_required and capacity on Start, a 5xx flipping the persistent bar and clearing after recovery); Docker image built and run locally (non-root, healthz 200, HEALTHCHECK "healthy", 179 MB); `.github/workflows/ci.yml` gained a `web` job (vitest, svelte-check, eslint, playwright with browser install, docker build + size + healthz check) — written but not executed on a real runner from here. DECISIONS I-79, I-62; RUNBOOK gained "dashboard up but API bar showing" and "sign-in loop". NOT done: sign-in against the *real* Logto with the real GitHub connector (needs a deployed Logto, same as every other workstream's "needs a deploy" items); access-token-refresh-failure and the SSE-route-missing failure rows (no way to force either against this fake); "via file" secret upload has UI but no Playwright coverage; Stripe SetupIntent against real Stripe test mode (no test keys here — the fake's `billing_disabled` path is what's covered); a real Coolify rolling-deploy curl-loop rehearsal; Lighthouse accessibility scoring; a human walking the built pages against a production account (the release-checklist "second human" item, not this workstream's alone). `features/config.md`/`secrets.md`/`snapshots.md` re-read against what the pages do, no mismatches found.
2026-09-20 | 13-notifications | ws/13-notifications agent (Sonnet 5) | claimed | most of the pipeline already exists from 04-guestd (hook socket, mapper, tmux-idle watcher) and 05-control-plane-api (events ingest, dedupe, rate limit, outbox, email/ntfy senders, notify-test): auditing that work against this doc, closing gaps (unsubscribe link, platform-event subjects, host_moved/snapshot_failed wiring, metric naming reconciliation), and the docs (features/notifications.md, DECISIONS, RUNBOOK)
2026-09-20 | 13-notifications | ws/13-notifications agent (Sonnet 5) | done (local) | Audited 04/05's already-built pipeline against this doc and closed what was missing rather than rebuilding what worked. Added: unsubscribe (`internal/api/notify/unsubscribe.go`, HMAC key auto-provisioned into the platform secrets like the CA material, `GET /v1/notify/unsubscribe`, email template now embeds the link, `internal/fakes/api` gained the route too); `notify.Subject` for platform-event subject lines (`billing_stopped`); `ops.EventSink` so `host_moved` (restore) and `snapshot_failed` (Snapshot command failure) actually reach `events`/the outbox, not just `projects.last_error` (both were previously log-only); `repose_api_notify_delivery_latency_seconds` metric. Tests: `TestEmailTemplate(WithoutUnsubscribe)`, `TestSubjectUsesPlatformWording`, `TestLoadOrCreateUnsubscriberProvisionsOnce`, `TestUnsubscriberVerifyRoundTrip`, `TestUnsubscriberRejectsTamperedToken`, `TestNotifyUnsubscribe` (full HTTP round trip), `TestNotifyTestRoute`, `TestSnapshotFailureNotifies`, `TestRestoreEmitsHostMovedEvent`. go build/vet/test ./... and golangci-lint run ./... both clean; `just done-check` shows only pre-existing panics/stubs from other workstreams. Docs: DECISIONS I-49 (tmux-idle heuristic never reads pane content, contrary to this doc's original §5.4 — 04 built it more privately than specified; metric naming is `repose_api_*` not `repose_notify_*`) and I-50 (checked Gemini CLI's and pi's current docs per this workstream's own "at time of writing" rule: both now ship real hook mechanisms, and separately Gemini CLI stopped serving free/Pro/Ultra-tier requests on 2026-06-18, replaced by Antigravity CLI — **flagged for the owner**, not resolved here, since mapping needs real binaries to verify against and the agent-list question is bigger than this workstream); `13-notifications.md` rewritten in place (§5.4, §5.6, §5.8, retry count, checklist) rather than left describing the original design; `features/notifications.md` rewritten to match built behaviour (was describing a time-based default-channel rule and a dedupe key that were never built, and cited a nonexistent `DECISIONS R4-15`); three `ops/RUNBOOK.md` entries added. NOT done: `billing_stopped` has no producer (workstream 09 is billing-exempt, I-16); the real-guest checklist item (no registered host yet, M1 blocked on the owner's apply); `repose status`/dashboard rendering (07/08's checklist items); Gemini/pi real hook mapping (I-50, needs the real binaries); the tmux window-exit case (needs a `remain-on-exit` change 02 would have to own, documented as "Not built" in §5.4 rather than silently skipped).
2026-09-20 | 14-security-followup | ws/14-security-followup agent (Fable 5.1) | claimed | H-2: guest@<id> transient units run as the hostd user (kvm, dm-* group access, guest dir group-accessible, --seccomp true explicit, NoNewPrivileges/ProtectSystem/DevicePolicy/RestrictAddressFamilies); no other High is open (H-1, H-3 fixed on ws/14-security)
2026-09-20 | 14-security-followup | ws/14-security-followup agent (Fable 5.1) | done (local) | H-2 fixed (DECISIONS I-51): guest@<id> runs Cloud Hypervisor as the hostd user with the sandbox pinned by internal/hostd/guest/testdata/unit.golden (NoNewPrivileges, no capabilities, ProtectSystem=strict, DevicePolicy=closed with /dev/kvm, /dev/net/tun and its own volume, private guests directory via TemporaryFileSystem+BindPaths, AF_UNIX/AF_VSOCK only), --seccomp true on the argv, hostd in kvm, udev rule making vg-guests/g-* group hostd, guest dirs 1770 root:hostd with a virtiofsd/ socket dir and --socket-group hostd. Evidence: go test ./... green, golangci-lint 0 issues, buf lint clean; VM test host-services runs a unit with the property list as hostd (uid 999, groups kvm): TUNSETIFF on its tap ok, unix socket in its dir ok, AF_INET refused (97), only its own guest dir visible, /dev/vhost-vsock refused by DevicePolicy (1), cloud-hypervisor --version runs; host-storage: /dev/vg-guests/g-test is root:hostd:660, x-test stays root:disk:660, dd through DeviceAllow on the LV symlink succeeds and fails without it; host-01 toplevel builds. Also fixed while getting there: the thin pool metadata clamp was never assigned (64 GiB disk got 0.64 GiB, >1.6 TiB would exceed the 16 GiB limit), the hostd stub read the value of --build-user as its command (host-services was red at HEAD 95349b5), and the VM tests now pin the stub explicitly. No other High is open (H-1, H-3 fixed on ws/14-security). NOT done: everything needing a host (I-51 verify list: systemctl show guest@<id> -p User, ps user of the CH process, a guest boot with ls /nix/store, test/isolation TestHypervisorRunsAsHostdUser); the dev box boot of the guest closure under the property list was written (scratchpad h2probe.sh) but needs sudo the session could not use. New residual L-11 (every guest's hypervisor is the same uid; taps) recorded in the review and SECURITY.md for 03.
2026-09-20 | m1-integration | ws/m1-integration session (Fable 5.1) | done (M1 gate reached) | On host-01 (Standard_D16s_v7, I-39): hostd registered with hostdev on the edge, guest 'todo' (large) created from the shared store, reached over SSH through edge and host with a CA certificate; closed on the real host: thin pool/lsblk/kvm/nested, bridge and nft tables, .links masked, IMDS blocked from a guest, github reachable, guest-to-guest blocked, docker overlay2, tmux, agents, hooks -> AgentEvent, chromium, noVNC chain, secrets/exec/principals/resize, samples, stop+snapshot to Blob, restore round-trip, apply in place (tmux survives), kernel change refused then forced, drain, hostd restart and kill -9 with replay, reconcile after a deleted state.db, nix-collect-garbage, second hostd refused, metrics, freeze p99 < 0.5 s, timings in RESEARCH §11. Integration fixes recorded as I-40, I-41, I-61..I-69 (register unit flag, host-net without WireGuard, store export propagation, virtiofsd socket dir and step-8 wait, image_type=raw, vsock CID, submounts, resize-disk, RegisterPaths, stale snap sweep, virtiofsd group, CLI subcommand flags). NOT done: Fluent Bit to Loki and node_exporter on wg0 only (no WireGuard/Loki until 06/10 deploy), Build/eval on the host (12's fragment pipeline not exercised), console rotation at 64 MB (fill did not reach it in the session), Ready under 5 s (11.7 s measured), certificate rotation at T-5 days on the host, the 12-hour guest cert reissue with the IP principal.
2026-09-20 | m1-integration | ws/m1-integration session (Fable 5.1) | follow-ups | (a) nix/hosts/tests host-services: the repose-register assertion needs a fake api in the test node now that hostModules installs the real hostd (the test pins the stub for the moment); (b) the runtime nft rule that lets the host reach a guest for SSH before WireGuard (ops/RUNBOOK.md 'Reaching a guest before WireGuard exists') needs a permanent home in nix/hosts (01) or goes away with 06; (c) hostdev state on the edge carries an empty 'drain-test' project record from the drain check; (d) hostdev serve stays running on the edge (transient unit hostdev.service, state /var/lib/repose-hostdev) for the M2 session.
2026-09-20 | 11-infra-opentofu (control plane) | ws/11-infra-control-plane session (Opus 5) | done (not applied) | Owner-scoped remainder, against the live prod environment (network, edge, host-01 already applied). Done: coolify_count = 1 in prod.tfvars with coolify_version pinned to 4.3.23 and AUTOUPDATE=false; cloud-init adds rclone, postgresql-client and /usr/local/sbin/repose-backup-check; terraform_data.ready holds the apply open until the coolify container's Docker health is healthy and fails if rclone or pg_restore is missing; a size validation rejecting pre-v7 SKUs (I-39's SkuNotAvailable); a postcondition on the control NSG that fails the plan on any inbound 8000/6001/6002/* (fired deliberately, message verbatim); manage_dns defaulted false in both roots with the Cloudflare zone id validated and `make plan ENV=r2` failing on the missing token by name; infra/r2 outputs coolify_s3_destination; docs/ops/coolify.md (the click path workstream §3 names, incl. the APP_KEY-is-half-the-backup point), runbook entries PostgresBackupStale / Coolify dashboard unreachable / ssh.repose resolves to Cloudflare, runbook first-time-setup corrected (it said `coolify_vm = true` and `https://<ip>:8000`), AZURE-SETUP steps 10 and 11, infra/README control-plane + DNS + lock-metadata sections, cost table re-derived 2026-09-20. DECISIONS I-71, I-71. Checklist evidence in the workstream doc §9: closed with real output are the state container, the clean base plan, host-01's registration (hostdev.service on the edge logged `host registered host_id=host-01a0bcd4` at 03:20:55Z; cert.pem/host.json/key.pem on the host; hostd `stream_connect`), /dev/kvm and nested on host-01, the empty nsg-hosts, the Blob lifecycle rule, and the cost re-check. TWO THINGS FOR THE OWNER: (1) herakraft.co answers every name from a proxied wildcard, so ssh.repose.herakraft.co resolves to Cloudflare today rather than to the edge - it carries neither SSH nor WireGuard, so the name is wrong rather than missing; the four records to create by hand are in infra/README.md "DNS while manage_dns is false". (2) the 2026-09-20 price re-query puts the D4s_v7 at $0.265/h, not the $140.16 a month the 2026-09-19 table carried, so the environment with the control plane on is about $1,094 and over the $1,000 budget alert in ops/AZURE-SETUP.md step 7. NOT closed, IMDS curl from the host (this box's sandbox refuses metadata fetches), the data-disk prevent_destroy attempt, the aged-blob deletion, Key Vault wrap/unwrap (needs api_identity_object_id), all of R2 and DNS (needs CLOUDFLARE_API_TOKEN), and the README end-to-end pass by somebody who did not write it. Nothing applied from this branch; two plans from this box lost their ARM connection mid-refresh and left a state lease, cleared by the conductor.
2026-09-20 | 11-infra-opentofu (control plane) | ws/11-infra-control-plane session (Opus 5) | done (not applied) | Owner-scoped remainder, against the live prod environment (network, edge, host-01 already applied). Done: coolify_count = 1 in prod.tfvars with coolify_version pinned to 4.3.23 and AUTOUPDATE=false; cloud-init adds rclone, postgresql-client and /usr/local/sbin/repose-backup-check; terraform_data.ready holds the apply open until the coolify container's Docker health is healthy and fails if rclone or pg_restore is missing; a size validation rejecting pre-v7 SKUs (I-39's SkuNotAvailable); a postcondition on the control NSG that fails the plan on any inbound 8000/6001/6002/* (fired deliberately, message verbatim); manage_dns defaulted false in both roots with the Cloudflare zone id validated and `make plan ENV=r2` failing on the missing token by name; infra/r2 outputs coolify_s3_destination; docs/ops/coolify.md (the click path workstream §3 names, incl. the APP_KEY-is-half-the-backup point), runbook entries PostgresBackupStale / Coolify dashboard unreachable / ssh.repose resolves to Cloudflare, runbook first-time-setup corrected (it said `coolify_vm = true` and `https://<ip>:8000`), AZURE-SETUP steps 10 and 11, infra/README control-plane + DNS + lock-metadata sections, cost table re-derived 2026-09-20. DECISIONS I-70, I-71. Checklist evidence in the workstream doc §9: closed with real output are the state container, the clean base plan, host-01's registration (hostdev.service on the edge logged `host registered host_id=host-01a0bcd4` at 03:20:55Z; cert.pem/host.json/key.pem on the host; hostd `stream_connect`), /dev/kvm and nested on host-01, the empty nsg-hosts, the Blob lifecycle rule, and the cost re-check. TWO THINGS FOR THE OWNER: (1) herakraft.co answers every name from a proxied wildcard, so ssh.repose.herakraft.co resolves to Cloudflare today rather than to the edge - it carries neither SSH nor WireGuard, so the name is wrong rather than missing; the four records to create by hand are in infra/README.md "DNS while manage_dns is false". (2) the 2026-09-20 price re-query puts the D4s_v7 at $0.265/h, not the $140.16 a month the 2026-09-19 table carried, so the environment with the control plane on is about $1,094 and over the $1,000 budget alert in ops/AZURE-SETUP.md step 7. NOT closed, IMDS curl from the host (this box's sandbox refuses metadata fetches), the data-disk prevent_destroy attempt, the aged-blob deletion, Key Vault wrap/unwrap (needs api_identity_object_id), all of R2 and DNS (needs CLOUDFLARE_API_TOKEN), and the README end-to-end pass by somebody who did not write it. Nothing applied from this branch. Final `make plan ENV=prod` at branch head against the live backend: 4 to add, 0 to change, 0 to destroy (the control-plane public IP, NIC, VM, readiness resource), no errors, one warning - the dns_is_managed_or_manual check, firing as designed. ARM reads from this box timed out twice during the session, each time leaving a state lease; both were cleared and the final plan released its own.
2026-09-20 | 01/03-host-followup | ws/01-host-followup session (Opus 5) | claimed | the two M1 follow-ups: (a) the runtime nft rule that lets the host reach a guest on 22 before WireGuard gets a permanent home in nix/hosts with a host-network assertion; (b) host-services registers against a real hostdev in the test node instead of the stub's fixture
2026-09-20 | 01/03-host-followup | ws/01-host-followup session (Opus 5) | done (local) | (a) I-74: `guest_in` starts with `ct direction reply ct state established,related accept`, so the host reaches a guest on 22 with no hand-inserted rule and the reachability survives `systemctl reload nftables`; host-network asserts nc to a guest's 22 with data arriving, ping, the same after a reload, and that a guest still cannot open 22 or 9100 to the host; host-conventions, SECURITY.md §2 and 14-security's boundary row, 01 §isolation, test/isolation's comment and the runbook entry all updated. (b) host-services runs the real hostd against a real `hostdev` in a second node (`api`: DHCP + 203.0.113.9:8443, state initialised at build time, only ca.pem crosses to the host through the new `repose.host.apiCAFile`, I-71): `hostd register` is a real gRPC registration (host_id, cert.pem, key.pem, token consumed once, hostdev's own `host registered` line and `token_used`), the bridge address comes out of the registration's guest_cidr, hostd then holds the stream (`connected: true`), `hostd status` and `repose-snapshot.service` answer on the control socket, and the PAM audit line carries the registered host id. Found on the way and fixed: I-72 (a running hostd never noticed an identity `repose-register` wrote; `TestEnsureIdentityTakesTheIdentityTheUnitWrote` fails on the old code), hostd was not on the operator's PATH although the runbook's `hostd status`/`guests`/`reconcile` need it, the audit assertion still grepped the stub's `audit_login` instead of `operator_login`, and two `grep -q` pipes that SIGPIPE their writer under pipefail. host-network and host-storage keep the stub (no api to register against). Evidence: host-network, host-storage, host-services all EXIT=0 on this box's KVM; host-services was red at HEAD (2e32fb1) on the first subtest, the stub's log line having no `ts`. NOT done: the real-host half (an operator SSH to a guest on host-01 with no manual nft rule; registration against the real api, 05), and host-services still tops up the registered host.json with the fixture's wg block and a test CA because hostdev registers a host without WireGuard or a Host CA (I-40).
2026-09-20 | 09-billing | ws/09-billing agent (Opus 5) | claimed | internal/billing (Stripe customer/SetupIntent/subscription, meter-event usage push, credit ledger, billing-period rollup moved out of internal/api/meter, cap and class-change rule, webhooks, dunning, reconcile, explain), migration 0003 (credit_ledger, stripe_events, usage_hours period/credit/price_version, users subscription/period/past_due_since), /billing/* routes incl. the webhook, repose-admin billing subcommands, the CHECKLIST fixture against a fake Stripe, docs
2026-09-20 | 09-billing | ws/09-billing agent (Opus 5) | done (local) | internal/billing: the rollup moved out of internal/api/meter and extended with the billing period anchored at signup (Stripe's short-month clamping, period_start/period_end on every row), the class-change cap rule, the trial-credit debit inside the insert's transaction and the trial-to-active transition, the per-part Stripe push; internal/billing/{period,ledger,rollup,stripe,webhook,dunning,reconcile,explain,enforce,config,prices,ports}.go; migration 0003 (credit_ledger with an append-only rule and a trigger-maintained projection on users, stripe_events, usage_hours period/credit/price_version, users subscription/anchor/past_due_since); POST /v1/billing/webhook with signature verification and event-id dedupe; the real Stripe portal/SetupIntent/invoices behind /billing/*; the Stripe customer at first GET /me; BILLING_ENFORCE with an audited flip; repose-admin billing rollup|credit|explain|reconcile|suspend|unsuspend|resync; two metrics, three alerts with promtool cases and runbook headings, three billing dashboard panels. DECISIONS I-77 (Stripe retired subscription-item usage records; the three cost parts travel as billing meter events with the identifier as the idempotency key) and I-62 (the rollup's home, the period, the ledger projection). Interfaces: db-schema.md, api.md. Evidence: go test ./... green (three consecutive full runs; one flake seen once in TestSignInAndProjectsLifecycle under parallel load, green 6/6 alone), golangci-lint 0 issues, buf lint clean, nix flake check --no-build all checks passed, nix build of api/repose-admin/hostd/hostdev/guestd/repose-hook (vendorHash updated for stripe-go v83), ops/check.sh ok, scripts/09-billing-names.sh 92/92. The CHECKLIST fixture (one large guest, 100 hours, 40 GB, 10 GB egress) asserts compute 1400 + storage 400 + egress 0 = 1800 cents less the 1000 trial credit = 800 pushed, against a fake Stripe that aggregates meter events the way a sum meter does. NOT done, all of it needing a real Stripe account: the test-mode transcript against Stripe itself and the test-clock run of the failed-payment path (the fixture and the 3-day stop are proven offline instead), the screenshot of a test invoice with a tax line, the live-mode charge of the owner's card, and step 17 of ops/AZURE-SETUP.md (the meters, prices and webhook endpoint a human creates). Also not done: the dashboard billing page (08) and `repose status` cost rendering (07).
2026-09-20 | 06-gateway-edge | ws/06-gateway-edge agent (Fable 5.1) | done (local) | Built without a staging edge: internal/gateway (SSH server + certificate auth chain per ssh-gateway.md; revocation/CA/route/gateway-cert caches with the §5.2 refresh and stale windows; terminate-and-redial relay with per-project 5-min gateway cert, session/direct-tcpip/forwarded-tcpip/agent relay, pty/window-change/env-filter/exit-status, keepalive, 24h cap; per-source auth limiter + fail2ban ban + connection cap; session reporting; wgsync; hook-ingest forwarder on :8443; preview stub), internal/gateway/preview, cmd/gateway (serve + wgsync), nix/edge (wg0 hub, nftables with exactly 22/443/51820 public + 2222/8443/6081/9100/9102 over wg, gateway + wgsync DynamicUser units, operator sshd on 2222 with PasswordAuthentication off per review M-2, node_exporter, fluent-bit, hardening). Gateway metrics folded into internal/obs/metrics.GatewayMetrics (DECISIONS I-81); relay close-ordering fixes (I-74). Merged main (obs/metrics/notifications wave); vendorHash updated for client_model becoming a direct dep. Evidence: go test ./internal/gateway/... green incl. relay of echo/pty/-L/-R/agent/exit-status with the x/crypto client, all §6 refusal banners, revocation within 30s, api-outage cache behaviour, 100-connection soak (no goroutine growth, all bytes relayed) run 5x; golangci-lint 0 issues module-wide; nix build of .#gateway and .#nixosConfigurations.edge.config.system.build.toplevel both succeed; hostd/api still build with the new vendorHash. Docs: DECISIONS I-81/I-74, api.md internal routes (I-4, present), 06 §5.5 metric names, runbook "Gateway relay failures" + GatewayAuthSpike reasons. NOT done (needs the staging edge / a real deploy, all marked in §9): nixos-rebuild onto the edge; wg0 up + wgsync add/remove against a real host; nft ruleset + external nmap; the guest-sshd two-guest cross-project reject; git push over the forwarded agent; VS Code Remote-SSH; sessions in GET /projects/:id on staging; gateway restart with tmux survival on a guest; the preview wildcard cert chain via openssl s_client; hook ingest from a real guest vs host source; the CI job running the OpenSSH binary against the in-process guest (harness is x/crypto client; the OpenSSH-binary CI matrix is added when CI runs on an edge).
2026-09-20 | conductor (waves 1-3) | orchestration session (Fable 5.1) | done | Wave three merged: 06-gateway-edge landed last (72650ad); every ws/* branch and worktree removed. CI green on main at 30bcb6b after four post-merge fixes: nix src filter keeps test/fake-logto (vendor step), CLI tmux test uses remain-on-exit (runner has no claude), dashboard Start tests wait for the page before stopping, api create reads its row inside the transaction. Next: M2 (docs/MILESTONES.md), blocked on owner steps: Coolify first-run (0 users), the four DNS records (still on the proxied wildcard), Logto apps.
2026-09-20 | 11-infra-opentofu (control plane) | orchestration session (Fable 5.1) | done (applied) | Owner correction (DECISIONS I-83): the control VM is a server of the owner's existing Coolify, not a Coolify install. cloud-init no longer runs the installer (Docker from Docker's apt repo, owner's Coolify key on root, readiness checks those); coolify_public_key in prod.tfvars, coolify_manager_cidrs in prod.local.tfvars widens the 22 rule; coolify_version/autoupdate/install_url removed from every root. Live VM converted in place: Coolify containers, network, images and /data/coolify removed, key added, new readiness checks pass by hand. Docs: docs/ops/coolify.md rewritten, RUNBOOK, infra/README, DESIGN, 11 doc. Not done: the owner adds the server in Coolify and supplies coolify_manager_cidrs.
2026-09-20 | 11-infra-opentofu (control plane) | orchestration session (Fable 5.1) | done | Owner decisions: Logto is the existing accounts.herakraft.co (I-84; auth.repose record and Logto step removed; api/dashboard/CLI defaults point there), Coolify reaches the VM over Tailscale (I-86; cloud-init installs it, owner runs `tailscale up`; coolify_manager_cidrs kept as fallback), 80/443 open to the internet (applied). Found and fixed on the way: the api verified iss as the bare endpoint, real Logto sends <endpoint>/oidc (I-85). Not done: owner joins the VM to the tailnet, adds the server, creates the Logto apps and M2M app.
2026-09-20 | control-plane deploy | orchestration session (Fable 5.1) | done (files written, not yet deployed) | Owner wants files over UI and one deployment per thing (I-87): ops/coolify/postgres/docker-compose.yml is Postgres + pg_dump/rclone backup services as one compose resource; api, api-grpc, web stay separate Dockerfile apps with ops/coolify/*.env.example to paste; api image gained HEALTHCHECK via `api -healthcheck` (distroless, Coolify's check cannot run inside); the grpc app issues its own server cert for GRPC_SERVER_NAMES (Validate() had demanded files nothing could sign); M2M app named repose-api. A first all-in-one compose draft was rejected by the owner (no rolling deploys) and reverted the same hour. CI validates the compose file. Not done: owner fixes Coolify->tailnet reachability (no SSH attempts reach the VM), adds the server, creates the four resources.
2026-09-20 | control-plane deploy | orchestration session (Fable 5.1) | done | Owner: Coolify already does backups, drop the sidecars. ops/coolify/postgres/docker-compose.yml is Postgres alone, added as a Coolify Service (pasted) so its Backups tab exists; pg-backup/backup-sync removed; docs and CI follow. repose-backup-check still verifies R2.
2026-09-20 | control-plane deploy | orchestration session (Fable 5.1) | done | Owner: Coolify overwrites container_name. Verified in Coolify's parser (bootstrap/helpers/parsers.php): container_name becomes <service>-<uuid> and network aliases are stripped; Compose's own service-name alias survives, so the service is now named repose-postgres and the Postgres service gets "Connect to predefined network"; apps are on the coolify network by default. Docs and env examples follow.
2026-09-20 | m2-integration | ws/m2-integration session (Fable 5.1) | claimed | M2 bring-up per PROMPTS.md: edge redeploy from main (gateway on 22, operator sshd 2222, wg0, wgsync) and control VM ⇄ edge WireGuard, api-grpc/internal reachable on 10.255.255.1 from the edge, host-01 registered with the real api (bootstrap-order decision), owner-run login/run gate with isolation checks, timings into RESEARCH.md
2026-09-20 | m2-integration | ws/m2-integration session (Fable 5.1) | progress | Branch pushed (I-92, I-93): edge-01.nix + static control-plane peer wgsync keeps, api /internal at 10.255.255.1:8444, repose-admin ca show/sign-server, control VM private IP pinned, host sshd binds wg0 once registered, hostd chowns the base checkout to nixbuild (first Build on host-01 failed on libgit2 ownership; fixed, then 35 s eval+build of a fragment on the host). Waiting on the conductor: merge, Coolify api-grpc port mappings + GRPC_SERVER_NAMES, apply with edge_operator_ssh_port=2222, edge nixos-rebuild. Then: edge files, tunnel, host-01 re-registration, gate.
2026-09-20 | m2-integration | ws/m2-integration session (Fable 5.1) | progress | Steps 1-2 done and verified: edge switched to main (gateway on 22 answering 'certificate required', operator sshd 2222, wg0 hub with the control VM as static peer, wgsync, hook ingest 8443, metrics on 10.255.0.1:9102; nft ruleset and external scan show 22/443/51820 only + 2222 to operators), control VM ⇄ edge tunnel up (1 ms), edge init recorded, api-grpc reachable at 10.255.255.1:8443/8444 with the platform CA and the gateway client cert (/internal/hosts answers; no cert refused). Step 3 in flight: host-01 switched to main (apiAddr 10.200.3.4:8443), hostdev identity retired, M1 guests destroyed, join token minted and placed in the owner's local tfvars; waiting for the owner's apply to deliver it.
2026-09-20 | m2-integration | ws/m2-integration session (Fable 5.1) | progress | Step 3 done: host-01 registered with the real api at 18:15Z (token delivered by the owner's apply through edge:2222), RegisterResponse carried the WireGuard material, wg0 up (10.255.0.2), wgsync on the edge added the peer and the 10.64.0.0/22 route, sshd/node_exporter/Fluent Bit bind the tunnel address only, 'repose-admin hosts list' shows host-01 ready; hostdev stopped on the edge (its state stays under /var/lib/repose-hostdev, hostd's old identity under /var/lib/repose/hostd/m1-hostdev); base 2026.09.20 published at main 49962ad. Next: hosts smoke, then the owner-run gate.
2026-09-20 | m3-web | ws/m3-web session (Opus 5) | claimed | M3 bring-up, web half per PROMPTS.md "M3 bring-up / m3-web": dashboard against the real Logto and api (08), rolling deploys of web and api, Postgres backups to R2 with a rehearsed restore, observability on the real path (10), runbook/features/security text rows. Creates no guests; project flows wait on m3's step 1 through the conductor.
2026-09-20 | m3-integration | ws/m3-integration session (Fable 5.1) | claimed | M3 bring-up, guests half per PROMPTS.md "M3 bring-up / m3": a project through the api onto host-01 and repose run; secrets in the guest tmpfs; menu and Nix (build log stream, in-place apply, takeover, kernel_changed, restricted-eval refusals, closure cap, GC roots); the five agents' completed events and delivery; api-grpc kill and restart under an op, base bump, snapshot expiry; the 14 boundary rows on host-01; M1 follow-up (a). Owns host-01 while it runs. host-01 is NOT ready (M2's join-token apply is with the owner, conductor 2026-09-20): starting with the local parts (step 7 check, isolation runner for step 6, secrets and menu scripts for steps 2 and 3, rows of 05/12/13/14 that STATUS and commits already close).
2026-09-20 | m3-web | ws/m3-web session (Opus 5) | progress | Closed against production, no guests created and no Coolify UI touched. **08:** apps/web/playwright.live.config.ts is a second Playwright project that runs against the deployed dashboard instead of internal/fakes/api; its `public` half is 11/11 green on https://repose.herakraft.co (landing install command; pricing table row by row against PRICING.md; the published privacy policy carrying the process-sample sentence verbatim and the terms the hosted-use phrases; the handover into the real Logto sign-in experience; §6's refused sign-in, bad callback and signed-out-route rows). Its `account` half needs one GitHub sign-in by the owner and then runs itself: `pnpm --filter web run live:auth` (opens a browser, owner signs in, saves tests-live/.auth/state.json, git-ignored, 0600) then `pnpm --filter web run live` — it round-trips settings and puts them back, asserts the delete-account guard without pressing it, forces a real refresh-token exchange, and never creates a project; sign-out is behind REPOSE_LIVE_SIGNOUT=1 because it revokes the saved session. **Lighthouse:** 100/100 on /, /projects and /projects/[id]/config (row wanted 90), after fixing the one audit they all failed — every page was a `<div>` with no `<main>` landmark, so "skip to main content" had nothing to jump to; the live landing page scores 100 on the deployed image too. Image rows closed from the running container: non-root `uid=100(repose)`, 170.8 MB against a 200 MB limit. **Rolling deploy (one of two):** measured, not rounded — ~10,400 responses across two loops at 5/s, exactly one failure each, both 5 s hangs at 18:05:15Z, eleven seconds after the new container started (image e6dd4fa); cause hypothesised as Traefik holding the outgoing container in its pool, not the health check, since the new container was healthy first. Recorded as docs/ops/coolify.md fact 13; `ops/deploy-probe.sh` is the loop. Second redeploy still owed. **10:** I-94, the edge's forward chain is policy drop, so every scrape of a host or of the control plane, and every Fluent Bit push to Loki, was dropped — `repose.edge.monitoring.{peerCIDRs,scrapePorts,logPorts}` adds exactly two rules when a monitoring peer is declared; ops/prometheus's api job pointed at 10.255.0.2, which is not the control plane's address (10.255.255.1, I-92), and is now two targets because api and api-grpc are two processes. I-95, `RegisterResponse` never carried `loki_url` although host.json, network.nix, fluent-bit.nix and the runbook all assumed it did, so every host would have shipped nothing; the api now fills it from a setting recorded by `repose-admin edge loki [URL]`, Rotate carries it, and fluent-bit.service refuses to start without one instead of retrying an empty host name for ever. With SSH tunnels standing in for the not-yet-existing WireGuard peer (ops/dev/tunnel-prod.sh + prometheus-prod.yml), all four real targets scrape and **16 of 43 Prometheus panel queries have real data**: every gateway panel including all 11 auth-failure reasons, 4 of 6 billing, both snapshot aggregates, the abuse egress panel. The 27 empty are 19 host-sourced (host-01 not registered yet), 2 Stripe (I-16) and 6 build/freeze histograms. All seven dashboards load into a real Grafana 12.4.0, no provisioning error. **14 text rows:** the two required passages are live at https://repose.herakraft.co/privacy and /terms and pinned both in the repo (test/isolation TestPolicyTextContainsTheRequiredPassages) and on the served pages; `rg credentials.json` is exclusions only (5 hits, all of them). **I-96:** features/status-and-logs.md and features/notifications.md promised a dashboard nobody specified (sortable list, sparkline, state timeline, ports card, per-meter cost, per-channel delivery status); corrected to what is live, with each promise moved to Deferred. NOT done, and why: the real-Logto sign-in rows (need the owner, commands above); the second web redeploy and any api redeploy (conductor's cue); all of backups — R2 token, `make -C infra apply ENV=r2`, the Coolify S3 storage from `tofu -chdir=infra/r2 output coolify_s3_destination`, the nightly schedule, one manual backup, `ssh root@<control ip> repose-backup-check`, and the timed restore rehearsal (nothing has ever landed in the bucket, and the VM has no `r2` rclone remote); Fluent Bit shipping from host-01 and the 27 host panels (host-01 is `registering`, per `repose-admin hosts list` through a docker exec on the api container); the edge nixos-rebuild that puts I-94's forward rules on the live edge. Two Coolify fields raised with the conductor: the `api` application has no port mappings at all, so its 57 live repose_* series are unreachable (README prescribes 9103:9103; api-grpc's 9104:9103 is in place), and api-grpc carries a stray default sslip.io domain pointing at port 3000 where it listens to nothing.
2026-09-20 | m2-integration | ws/m2-integration session (Fable 5.1) | gate ready | `repose-admin hosts smoke host-01` passed through the real api and hostd (build 18.8 s, create 21.5 s, snapshot 16.5 s, stop 4.5 s, start 14.5 s, destroy 22 s) after I-97 (/run/repose 0700 from the token delivery broke virtiofsd's traverse; fixed by hand on host-01 and in the provisioner, host-net and runbook). OWNER-RUN GATE, exact commands for the owner and for a second person, each on their own laptop (macOS or Linux with git and ssh; a GitHub account): (1) CLI: `nix run github:Heracraft/factory?dir=nix#repose -- version` or `go install github.com/heracraft/repose/cmd/repose@main` (no release exists; install.sh points at heracraft/repose, which is not this repository). (2) `repose login` (browser; or `--no-browser` for a device code) and tell the M2 session your GitHub handle; the session runs `repose-admin users exempt <handle>` (I-16; until then `repose run` exits 7 with "add a card"). (3) `repose notify set --ntfy https://ntfy.sh/<a private topic>` and `repose notify test` (email needs RESEND_API_KEY, unset today; ntfy needs nothing). (4) in a git checkout with an origin remote (a small public repo): `repose run` — creates the project, builds its guest on host-01 (first build minutes), boots it, syncs, attaches to tmux; `Ctrl-b d` detaches. (5) `repose run "say hello in the terminal, then stop"` — opens a Claude Code window; the first time it attaches for the Claude login inside the guest instead of sending the prompt: finish the login and repeat; the finished-agent notification arrives on the ntfy topic. (6) `repose status`, `repose attach`, `repose open 3000` (if something listens there), `repose stop`, `repose start`; and from a plain terminal `ssh <slug>.repose`. The second person, once the owner has a running guest, also runs `ssh <owner slug>.<owner handle>@ssh.repose.herakraft.co` (expected: "certificate not valid for this project") and `repose --project <owner slug> status` (expected: not found).
2026-09-20 | 07-cli (release) | orchestration session (Fable 5.1) | done | I-98: install.sh points at Heracraft/factory, release.yml publishes on v* tags, the web build serves /install.sh; v0.1.0 tagged. Gate step 1 is the documented curl | sh once the web image is redeployed from main.
2026-09-20 | 14-security | ws/m3-integration session (Fable 5.1) | review 05 | secrets handling reviewed, no findings: AES-256-GCM per-user DEK with the name as AAD, DEK wrapped by Key Vault (Get/WrapKey/UnwrapKey, I-91), plaintext only in DecryptForGuest's callers (the four hostd command builders, the platform CA rows, and the build-log redaction set held for the build's duration and cleared when the op ends), user route enforces [A-Z][A-Z0-9_]{0,63} and refuses the reserved names (closes L-5 api-side), GET /secrets has no value field, the secret_put log line and both audit rows carry project id and name only. docs/security/review-2026-09-20.md "Secrets handling review".
2026-09-20 | 14-security | ws/m3-integration session (Fable 5.1) | review 07 | credential sync reviewed: an allowlist of exactly the four guest-conventions rows over the user's own SSH session as a one-file tar with mode 0600 (0644 for .gitconfig), never Claude, Gemini or SSH keys (pinned by run_integration_test.go), the CLI's own refresh token 0600 or in the keychain. One finding, L-12 (Low, 07): the mtime rule of features/secrets.md is not implemented; a login done inside the guest is overwritten by the next `repose run`. Also recorded L-13 (Low, 05): no audit_log producer for an operator SSH login (journal line only) or for a restore through the user route.
2026-09-20 | m1-integration follow-up (a) | ws/m3-integration session (Fable 5.1) | done | nix/hosts/tests host-services registers the real hostd against a real hostdev in a second node (01/03 follow-up, I-75, I-76); verified on this box's KVM 2026-09-20 18:11Z: `nix build ./nix#checks.x86_64-linux.host-services` exit 0, 21 subtests, hostdev logged `host registered host_id=host-01a0c004`, hostd `registered`, `connected: true`, token consumed once and a second token refused, `hostd status` on the control socket, nightly snapshot unit ran, operator login audited with the registered host id. Nothing left of (a).
2026-09-20 | m3-integration | ws/m3-integration session (Fable 5.1) | progress | Local parts done while host-01 is M2's (conductor): ops/checks/ (secrets.sh, menu.sh, notifications.sh, resilience.sh, isolation-host01.sh, audit-rows.sh, lib.sh, fragments/, README.md) written and shellcheck-clean, each driving the real api through the CLI or its token, the guest through the gateway, host-01 through the edge on 2222 and repose-admin/Postgres through docker exec on the control VM, writing evidence blocks worded like the checklist rows; 05, 12, 13 and 14 §9 ticked from local evidence with the script that closes each host half named; 14 secrets reviews of 05 and 07; review L-12, L-13; M1 follow-up (a) verified. main merged at 4bcc94b. NOT done, all waiting on "host-01 ready": steps 1 to 6 on the host (the scripts are the plan), timings into RESEARCH.md, the owner's ntfy URL and a second person's project for steps 4 and 6, a kernel-changing base rev for the kernel_changed row (ops/checks/README.md).
2026-09-20 | m3-web | ws/m3-web session (Opus 5) | progress | Second half of the session, after the first STATUS line. **Rolling deploys, both rows closed as measured rather than as zero:** seven switchovers landed inside the probe window (five web, two api) and every one lost one or two requests per client, over 29,841 responses and 14 failures. Seven for seven, and the delay is identical per application — web +11/+12 s, api +26 s, both times — which is each image's HEALTHCHECK start period, so the failure lands when Coolify removes the old container, not when the new one starts. The health check is right; what is missing is a drain, Traefik holding the outgoing container in its pool. Almost all failures are 5-second hangs; one was a clean 502, the same gap once the container is actually gone. `docs/ops/coolify.md` fact 13 has the table and how to read a probe afterwards; RUNBOOK's "Coolify deploy failed" gains the step that tells this apart from an outage. On web it is a slow page; on api it is a CLI command or a dashboard poll that fails, which is the owner's decision (the lever is Traefik's drain on the Coolify proxy, not our images). **I-95:** the api never sent `loki_url` although host.json, network.nix, fluent-bit.nix and the runbook all assumed it did; `RegisterResponse` field 7, filled from a setting recorded by `repose-admin edge loki [URL]`, carried again by Rotate, with fluent-bit.service refusing to start rather than retrying an empty host name. Live and verified on the deployed image: `docker exec <api> repose-admin edge loki` answers "no Loki recorded; hosts ship nothing". **I-96:** features/status-and-logs.md and features/notifications.md promised a dashboard nobody specified; corrected to what is live, promises moved to Deferred. **14 §9 row 1 closed:** the SECURITY/ARCHITECTURE boundary diff review, by someone other than the author — four of five map, the fifth (operator to tenant data) correctly has no row because it has no mechanism, and ARCHITECTURE's guest-to-host row was stale from before I-51 moved Cloud Hypervisor to the hostd user. **api metrics, corrected in the other direction:** I reported the missing `9103:9103` mapping on the `api` app as a gap; the owner caught that a published host port stops the old and new containers coexisting, so it would have cost the api its rolling deploys, which is exactly what I-2's split protects. The README line is gone, coolify.md fact 14 says why, prometheus.yml drops the target rather than carrying one that can never answer, and both replacement paths (a Traefik router with an ipallowlist, label block ready to paste; or Grafana Alloy on the coolify network) are written up with the case against each in ops/coolify/README.md "The api's metrics". Not implemented: the owner chooses. The decisive caveat is that `ipallowlist` matches the source Traefik actually sees, and the failure of getting that wrong is silent and open. **`ops/restore-rehearsal.sh`:** the R2 restore as one timed command into a throwaway postgres of its own — never repose-postgres, no published port, removed on exit — tested end to end here on both dump shapes (custom 59K, gzipped SQL 6.2K), both ending in db status [1 2 3] and a full verify; the R2 fetch is the one path this box cannot exercise. **17 alert rules** load and evaluate healthily against real production series, none firing, none in error. **Live suite** grew the api's side of the browser contract (401 envelope on four routes, CORS preflight and response headers) and the install.sh assertion after I-98 made it real; 14/14 green against production, re-run after each deploy. **CI** runs the Lighthouse audit now, reports uploaded every run. Gates on the merged branch: go test ./... , go vet, golangci-lint 0 issues, buf lint, nix flake check --no-build, ops/check.sh, just done-check (only pre-existing generated-code and commented-fixture hits). STILL OWED, all owner-gated: the signed-in half of the live suite (one `pnpm --filter web run live:auth` on a machine with a browser); everything in backups (R2 token → infra/r2 apply → Coolify S3 storage → nightly schedule → one manual backup → repose-backup-check → restore-rehearsal.sh); the WireGuard peer and Loki URL that turn the 27 empty host panels and Fluent Bit shipping on; the edge rebuild carrying I-94's forward rules; and the api-metrics choice.
2026-09-20 | m3-integration | ws/m3-integration session (Fable 5.1) | progress | main merged at de1bd12 (my first batch is on main at faeaefc). kernel_changed plan per the conductor (no nixpkgs bump): test base 38d1cbf flips only boot.kernelPackages to linuxPackages_latest (7.2.6, cached), reverted at once by 741e1e0; ops/checks/README.md has the three-step publish plan, run by resilience.sh bump with BASE_REV=38d1cbf once the conductor approves. ops/checks/.shellcheckrc makes a plain shellcheck run match the flagged one. Still waiting on: host-01 handover, the owner's ntfy URL, the second person's slug and handle.
2026-09-20 | 07-cli (gate fix) | orchestration session (Fable 5.1) | done | I-99: `repose login` sent "repose-cli" as client_id; Logto wants the App ID. Default jccig5bb3i4d78bq4farv, `logto_client_id` config override, recorded in credentials.json; v0.1.1.
2026-09-20 | 07-cli (gate fix) | orchestration session (Fable 5.1) | done | I-101: `repose login` defaults to the device-code flow; `--browser` is the loopback PKCE flow, which the Logto Native app cannot serve (no redirect URIs). v0.1.2.
2026-09-20 | 07-cli (gate fix) | orchestration session (Fable 5.1) | done | I-102: device-code and refresh requests now carry resource=api; the api had refused the opaque token. v0.1.3.
2026-09-20 | m3-web | ws/m3-web session (Opus 5) | progress | Owner's backup decision applied (I-103): Coolify owns the backups, the destination is an S3 storage in the owner's own Coolify, and no credential for it comes through this repository, the control VM or an agent session. Changed: `repose-backup-check` no longer lists a bucket through rclone — it reports the age of the newest dump Coolify wrote under `/data/coolify/backups` and says in its own output that this proves the dump was taken, not uploaded (kept rather than deleted, because a dump that was never taken is the failure that silently leaves no restore point and is invisible until someone looks); `ops/restore-rehearsal.sh` takes the dump file downloaded from the Backups tab as a bare argument, with `--from-bucket` left for whoever has a remote of their own; `backup_bucket` removed from infra/azure/modules/{environment,coolify} and `backup_max_age_hours` kept; `infra/r2` stays as an optional module with a header saying production does not use it; AZURE-SETUP step 10, ops/coolify.md ("The Postgres backup", "Restore rehearsal", the human-steps list), ops/coolify/README.md, RUNBOOK (PostgresBackupStale rewritten around the two halves and which tool answers which, "Postgres restore", first-time setup) and CHECKLIST's restore item all follow; the m3-web block in PROMPTS.md no longer asks for a token. Fixed while in the same paragraphs, both contradicting merged decisions: RUNBOOK's first-time setup still told an operator to set `api`'s pre-deploy command to `repose-admin db migrate` and to run `ca init` after the first deploy (I-90 made both automatic and a pre-deploy command does not run on a first deploy at all), and still said "Connect to predefined network" is on for the Postgres Service (I-89 turned it off; the compose file joins the network itself). Evidence: the rewritten check extracted from the cloud-init template and exercised on all four paths (missing directory, empty directory, fresh dump, 40-hour dump against a 36-hour and a 48-hour limit) with the right exit codes and messages; restore-rehearsal.sh re-tested on a bare-path dump, `--dump`, no arguments, two bare paths and `--from-bucket` without rclone, leaving no container behind; `tofu validate` clean on infra/azure/prod, infra/azure/staging and infra/r2; ops/check.sh ok.
2026-09-20 | m2 e2e from the dev box | orchestration session (Fable 5.1) | progress | Owner asked for the CLI to be tested end to end from this box before releasing. Done so far on a build of main: device login (I-99, I-101, I-102 fixed on the way), notify set/test to ntfy delivered, `repose run` created the project and host-01 built and booted the guest (small) in about a minute, status shows it running. Found and fixed: create-then-start conflict (I-106), guest repo with no origin (I-107, guestd, needs a base bump to reach existing guests), plain-terminal ssh offering the agent key first (I-108), tz abbreviation (I-104). Pending: sync into the guest with origin added by hand, attach, stop/start, ssh alias, the finished-agent notification; then destroy the test project so the owner's row can be renamed (I-105).
2026-09-20 | m2 e2e from the dev box | orchestration session (Fable 5.1) | progress | On base 2026.09.20.2 (d315339): guestd sets origin (I-107 works), the ssh include is gone (I-109 works), `repose run` waits on the create op (I-106 works), sync completed over an HTTPS origin (tree at the right commit, credentials copied), stop with snapshot 22 s, start, ssh alias, gateway sessions counted. Found: the guest lacks github.com in known_hosts so an SSH origin fails host-key verification (I-111, fixed in the guest base, needs the next base); the gateway never closes an exec once agent forwarding was used (I-110, with the M2 session); two parallel CLI scripts hit the 60/min token rate limit, which a single client does not.
2026-09-20 | m3-web | ws/m3-web session (Opus 5) | progress | Owner's second and third backup corrections applied (I-112, superseding what was left of I-103): backups are entirely Coolify's, against a destination in the owner's own Coolify, and this repository has no part in them. Removed rather than reworded, because a half-owned responsibility is the one nobody holds — `repose-backup-check` and `backup_max_age_hours` out of the control VM's cloud-init and both infra modules; the readiness provisioner no longer asserts `rclone`/`pg_restore` and cloud-init no longer installs them, since the restore procedure that justified them is gone; `ops/restore-rehearsal.sh` deleted; `infra/r2` deleted with the Makefile's `ENV=r2` root and its Cloudflare token guard (a token is still needed for `manage_dns`, a different scope); the backup and restore sections out of ops/coolify.md, ops/coolify/README.md, AZURE-SETUP step 10 and RUNBOOK (PostgresBackupStale and "Postgres restore" both gone); CHECKLIST's item is now "configured in the owner's Coolify; nothing here"; the m3-web and 11 blocks in PROMPTS and 11's checklist row follow. Second half: Coolify redeploys on every push to main, so "then redeploy X" is never an instruction — removed everywhere, recorded as coolify.md facts 15 and 16, with two survivors that are not routine deploys (a rollback from Coolify's deployment history, and a re-paste of the Postgres Service, which is pasted rather than watched). Consequence worth knowing: fact 13's dropped request happens on every merge, not only on deliberate deploys. Two judgement calls flagged to the conductor rather than taken silently: kept ops/coolify.md's APP_KEY note ("the instance's .env is half any backup"), because it is a fact about the owner's own restore rather than backup machinery and matters more now the procedure is wholly theirs; and there was no PostgresBackupStale Prometheus rule or dashboard panel to remove — only the runbook heading existed, which is gone. Evidence: `make -C infra fmt-check validate` clean across azure/prod, azure/staging and bootstrap; ops/check.sh ok (17 rules, 7 dashboards, 59 panels, every alert still has a runbook heading); `make plan ENV=r2` now fails with "ENV must be prod or staging"; no reference to repose-backup-check, restore-rehearsal, PostgresBackupStale or infra/r2 survives outside DECISIONS and this file, which are the record.
2026-09-20 | m2 e2e from the dev box | orchestration session (Fable 5.1) | done | CLI proven end to end against production from a build of main: device login (I-99, I-101, I-102), notify set/test delivered to ntfy, create with the CLI waiting on the create op (I-106), build + boot on host-01 (~1 min), sync over an SSH origin through the forwarded agent (I-107, I-110 on the switched edge; I-111 stood in per-repo), stop with snapshot (22 s), start, `ssh <slug>.repose` (I-108), gateway sessions counted, tz (I-104), guest ssh include (I-109). Not exercised here: an agent finishing and its notification (needs a Claude login inside the guest; the owner's gate does it), and a second person's isolation rows. Test projects recruiting and nuru destroyed by the M2 session; the owner's row renamed to heracraft (I-105 for future rows). Base from 65d336e carries I-111 for the owner's first guest.
2026-09-20 | m2-integration | ws/m2-integration session (Fable 5.1) | progress | Gate rehearsal by the conductor found and main fixed I-99 (CLI client id), I-101 (device code default), I-102 (resource on the device grant), I-104 (tz), I-106..I-109 (create conflict, guest origin, IdentitiesOnly, ssh_config.d over virtio-fs), I-111 (github host key); this session fixed I-105 (provisioner reads Logto's rawData.userInfo.login; the fake Logto now renders the real shape) and I-110 (relay half-close towards the client; an exec with -A returned in 0 s on the switched edge after a 25 s timeout before). Bases published: 2026.09.20 (49962ad), .2 (d315339), .3 (65d336e, latest); smoke on .3: build 38.6 s, create ok, snapshot 24.6 s, then the api redeploy (every push to main redeploys it) cut the smoke client; stop/start/destroy were proven on .2. Test projects recruiting, nuru and the smokes destroyed; host-01 ready with 0 guests; repose-admin gained users rename/projects destroy (I-100) and rename ignores destroyed rows. Pending: rename user-yv7ryeiczbkz -> heracraft once main carries the guard fix; then the owner's and the second person's runs.
2026-09-20 | m3-web | ws/m3-web session (Opus 5) | progress | Edge redeployed to main; **the I-94 forward chain is shipped but inert**, and a next session should not read the redeploy as closing anything. The rules are conditional on `repose.edge.monitoring.peerCIDRs`, `nix/edge/edge-01.nix` does not set it (it needs the monitoring server's WireGuard public key, still owed by the owner), and the live edge's forward chain is verbatim what it was: `ct state established,related accept` then drop. So 10's "the api's, edge's and host's metrics are scrapeable over WireGuard" **stays open**; when the key arrives it is a `staticPeers` entry plus `monitoring.peerCIDRs` in edge-01.nix and one rebuild. What did close: host-01 is now a WireGuard peer of the edge (10.255.0.2/32 plus its guest /22) and its three exporters answer — node_exporter 200, hostd 200 with 195 repose_host_* series, Fluent Bit 200 on /api/v1/metrics/prometheus — so the exporters and the panel queries are proven even though the peer path is not. With the SSH tunnels extended through the edge to reach them, six targets up against production and **38 of 43 Prometheus panel queries return real data (was 16)**. The distinction matters and is written into `ops/dev/tunnel-prod.sh`'s header: the edge originates a tunnelled connection itself, so it is `output` and never crosses the forward chain a real scrape would. Remaining 4 empties are honest: two Stripe (I-16) and two build-*failure* panels with no failures to show. Two address guesses corrected against `wg show wg0 allowed-ips`: targets/hosts.yml had host-01 at 10.255.0.7 and wireguard-peer.conf claimed ".4 and up for hosts"; the allocator gave .2. **Handed to m3/03:** `repose_host_guests` has no series at all on a host with three guests, not even a HELP line, so Host capacity's "Guests by state" is blank in production; everything else from the same metrics struct is present so it is not a nil registry, and the hypothesis (untested, hostd is not this session's to restart) is startup order — `refreshGuestGauge()` returns early on `m.d.Metrics == nil` during reconcile and nothing calls it again until a guest changes state, which is exactly a host that has been up a while with stable guests.
2026-09-20 | m2-integration | ws/m2-integration session (Fable 5.1) | gate running | user-yv7ryeiczbkz renamed to heracraft (exempt); final gate text sent to the conductor for the owner (base 2026.09.20.3, CLI v0.1.3, switched edge with I-110) and the second person (GitHub login, handle -> users exempt, repose run, the two cross-project refusals, own ntfy topic). Verification from the host and api side follows their runs.
2026-09-21 | 07-cli (finding) | ws/m3-integration session (Fable 5.1) | open | The CLI has no re-issue path on a revoked certificate: ensureCert reuses the on-disk certificate while it has validity left, and a gateway refusal ("certificate revoked") ends `repose run` at the SSH step (seen on this box after test/isolation's revocation row, 2026-09-21 00:16Z). 07 §9's "gateway rejects certificate, retry once" row: the refusal should trigger one POST /certs re-issue, then fail. Recovery today: `rm ~/.ssh/repose/id_ed25519-cert.pub && repose run`. Conductor's call: fix later, not tonight.
2026-09-21 | 05/12 (finding) | ws/m3-integration session (Fable 5.1) | open | `repose-admin base publish --rev` accepts any string: a short or mistyped sha (2026.09.21 was published as `3f83664f`, which does not exist) reaches hostd, whose clone fails with `pathspec did not match`, and every create on that base fails until the next publish. It should resolve a short sha through the platform repository or refuse it, and refuse a rev the repository does not have (the M2 finding again). The row 2026.09.21 stays in base_versions, superseded by 2026.09.21.1 (full sha).
2026-09-21 | m3-integration | ws/m3-integration session (Fable 5.1) | progress | On host-01 through the real api (owner login on the dev box, projects m3-check and m3-stamp; synthetic tenants repose-m3-a/b through `repose-admin projects create`, I-113): step 1 done (create to running 47 s, 84 s through the CLI); step 2 done (secrets.sh: 0400 dev on tmpfs, login-shell export, gone within 5 s, refused in a fragment, value in no route, log, build log, event or audit row); step 3 done but (a) and kernel_changed: menu apply of bun in 5 s with the SSE log, on PATH with no reboot, selection line and takeover 409 (after I-119), the four eval refusals with the contract's lines, a fixed-output fetch, GC roots (I-115 fixed the rev-* leak), closure cap 26.6 GB with ten paths, timings in RESEARCH §13; the syntax row waits for the I-126 api deploy, kernel_changed for the approved 38d1cbf publish after "gate done"; step 4: claude/codex/opencode events delivered to ntfy and email in 1 to 7 s (replayed hooks, no agent logged in), pi heuristic 101 s, gemini none until I-122/I-125 ship (node's MainThread); step 6 done: 18 isolation rows PASS on host-01 with two tenants (neighbour ratio 1.02 under a fork bomb and memory hog), revocation row passed once then opt-in, direct-sshd half skipped by design, password attempts refused and logged on host and edge, audit rows for cert issue/revoke, secret put/delete, exec, project snapshot/create/destroy; step 7 verified. Defects found and fixed on the way: I-113..I-128 (CLI create race and blank errors, rev-* roots, gauge zeros, base-version stamp, the menu stand-in, tap/MAC collision from the UUIDv7 prefix, AgentEvent window, gemini as node, empty-plan destroy, parse-time wording, op re-read after the stream, the verbatim block). Open: step 5 (resilience.sh waits for "gate done"), kernel_changed row, the syntax row on the deployed api, gemini after the next base, the revocation row against a non-owner account, hostd-X-vs-host-Y (one host), findings for 07 (no re-issue on a revoked cert) and 05/12 (base publish accepts a bad rev), L-13 audit producers, the base-version stamp of guests built before I-118 reads dirty until their next build. Branch pushed; the conductor merges after the owner's gate.
2026-09-21 | m2-integration | ws/m2-integration session (Fable 5.1) | gate done | M2 closed on the owner's call at 01:02Z with one person plus a second account (DECISIONS I-129; MILESTONES M2 gate annotated): the owner's laptop run through the released CLI v0.1.4 (device-code login on user-c7fh26yzrl93, nuru-playground and age-calculator created, built and running on host-01, relays from the laptop through the switched edge), m3's isolation suite under two accounts (18 rows pass), and the ntfy deliveries. 06 and 07 real-host rows closed with evidence in their §9; RESEARCH §12 carries the timings. Left open: a second human's laptop (M5 step 3), hook ingest from a real guest, the preview wildcard certificate, VS Code Remote-SSH, open PORT/--desktop on a real laptop, wgsync peer removal for a retired host; per-relay session id (I-123), base publish rev validation, install.sh repo name (fixed by I-98), gateway_sessions keyed by serial. Not touched: the owner's two guests. Edge runs I-110 (I-123 lands at the next edge switch); host-01 on d3568d4; hostdev stopped; watches stopped.
2026-09-21 | m3-integration | ws/m3-integration session (Fable 5.1) | progress | Step 5 on the real api: api-grpc SIGKILLed mid-build, Docker restarted it in 6 s, hostd reconnected in 33 s, the build op finished with no lost command; api restarted mid-snapshot, the op re-driven and done in 15 s, an attached session saw 0 gaps. `docker kill` is not that row: unless-stopped treats it as a stop (three minutes down at 01:15Z; RUNBOOK "api-grpc stopped and did not come back"). Expiry: two m3-check snapshots aged to 8 days, the run refused at its first blob delete with 403 AuthorizationPermissionMismatch (01:27:06Z), so the api's identity had never had Blob delete (conductor's I-131, awaiting apply) and the run stopped at that first error (I-130, fixed: one transaction per row, refused rows logged and kept, run reports the count). Step 4 gemini row closed on base 2026.09.21.2 (I-122/I-125). New 05 row "expiry has deleted a real blob in production" open until the rerun after the apply. Still open: kernel_changed/bump (owner's choice on the m3-held sweep), the revocation row against a non-owner account, hostd-X-vs-host-Y (one host), findings 07 (no re-issue on a revoked cert), 05/12 (base publish rev validation), L-13.
2026-09-21 | m3-integration | ws/m3-integration session (Fable 5.1) | progress | Expiry closed on the real path after I-131 (02:00Z rerun: refused rows deleted, the restore's snapshot held, newest kept). Kernel row, option (b): the 02:08Z security publish of the kernel-flip base 38d1cbf swept nuru-playground, age-calculator, m3-check and m3-iso-c at 02:12Z and skipped m3-held, but every build ran against the base the project was leaving (5 s builds, kernel_changed false, no new checkout on host-01) because the build phase took its base from the project row, not the bump's revision: I-134, fixed with `TestBumpNeedingRebootSaysSo`; the same test drove I-132 (the base_updated summary said "applied" for a revision that ended built with reboot_required). The owner's two guests untouched (unit start times unchanged, still running). The kernel row reruns with a second publish from the same rev after the api deploy carrying I-134, then the LTS republish.
2026-09-21 | 11-infra-opentofu (conductor) | conductor session | needs apply | I-131: the api's service principal gets Storage Blob Data Contributor on the snapshots container (`modules/storage` `api_identity_object_id`, passed by `modules/environment`), after the first production expiry run was refused with 403 AuthorizationPermissionMismatch at 01:27Z. `tofu validate` and `fmt` clean; the owner applies from infra/azure/prod; the 05 expiry row stays open until the rerun after the apply.
2026-09-21 | m5-release | ws/m5-release session (Fable 5.1) | claimed | M5 bring-up per PROMPTS.md: step 1 (adversarial review of main as deployed on host-01 and the edge: SECURITY.md boundaries, audit log, operator access, never-log list against live journals and Loki; findings fixed on the branch or filed as DECISIONS entries with owner and date) and the owner-free parts of step 4 (repose --version, install.sh targets, dashboards and alerts to the owner's stack with runbook rows, privacy and terms, the b1a5915 decision recorded), then the CHECKLIST 'Release (M5)' rows those close. Steps 2 and 3 (second host, second human) wait on the conductor's scheduling. Read-only on host-01 and the edge unless announced; no infra apply, no Coolify UI, no api restarts; the owner's nuru-playground and age-calculator untouched; test projects under repose-m5-* handles, at most two alive.
2026-09-21 | m3-web | ws/m3-web session (Opus 5) | progress | Overnight batch, nothing needing the owner. **I-133** (owner's call via the conductor): the api's /metrics is a Traefik router on the app behind an ipallowlist, not a collector and not a host port — the label block in ops/coolify/README.md is now the documented paste rather than one of two options, prometheus.yml's api job is restored as the *name* (the router matches on Host, so the monitoring server needs one /etc/hosts line pointing api.repose.herakraft.co at 10.255.255.1, documented in wireguard-peer.conf step 4), and `repose.edge.monitoring.scrapePorts` drops 9103 for 443. Alloy recorded as rejected with the real reason: a second resource for one endpoint and a remote_write receiver the owner's stack does not expose, i.e. changing their stack to suit ours; the argument survives for the day Fluent Bit is reconsidered. Live proof waits on the owner pasting the labels, and closing 10's scrape row must include `curl https://api.repose.herakraft.co/metrics` from outside answering 403 — the allow-list matches the source Traefik sees, and getting that wrong fails open and silently. **Checklists closed with evidence**, which was the real gap: 08 §9 had 19 open rows and 10 had 10, none ticked, though most of the evidence existed. Now 16 of 08's and 8 of 10's are closed row by row in the house style, and every row still open names what it waits for. **Two rows were genuinely uncovered and got tests rather than ticks:** `src/lib/poll.test.ts` (8 tests on fake timers — 10s/60s intervals, the 60s backoff while the api is unreachable, a visibility change rescheduling immediately, stop(), no overlapping polls) and two more in tests/secrets.spec.ts for the file path (a non-UTF-8 byte sequence stored and absent from the DOM, and a file over 64 KB refused). Writing the first found a comment in reachability.svelte.ts that denied what the code does — it said a network failure does not change the interval, while pollWhileVisible reads `ok` to choose 10s or 60s, which is what §6 asks for; the code was right and the comment now names the test that pins it. **Stale gate text fixed:** MILESTONES' M3 gate still demanded "Postgres backs up to R2 nightly and a restore has been rehearsed" after I-112 withdrew all of it; it now gates on the schedule existing. Two CHECKLIST release rows (seven dashboards, alerts) closed with evidence, the alerts row saying plainly that "wired" means loaded and tested, not yet delivering. STILL OPEN, all four needing the owner: the real-Logto sign-in (one `pnpm --filter web run live:auth`), Stripe test keys (arguably M4's), the Loki URL, and the monitoring server's WireGuard public key — the last also gates the 403 check and an edge rebuild, since I-94's rules are shipped but inert with no peer declared. Gates: playwright 30/30, vitest 30/30, eslint, svelte-check 518 files 0 errors, go vet, golangci-lint 0 issues, buf lint, nix flake check all passed, ops/check.sh ok, just done-check only the pre-existing hits.
2026-09-21 | m5-release | ws/m5-release session (Fable 5.1) | progress | Step 1 done as far as one host allows: docs/security/review-2026-09-21.md (main as deployed on host-01, the edge and the control VM, read-only; every boundary row re-read against the machines). Fixed and merged: I-136 (the api served /metrics on its public listener, 200 from the internet; 404 in production since 02:30Z), I-137 (hostd's second wg0.conf with the WireGuard key; reaches host-01 at the next switch), repose --version, /privacy and /terms prerendered, I-138 (--name projects failed at sync). Closed on the machines with the conductor's go-ahead: M-4 (hostdev's M1 state removed from the edge and host-01, 02:20Z). Verified closed: M-2 (edge sshd hardened). Open with owners and dates in the review: M-1 (no Host CA on hosts; 750 bootstrap-key logins/24h), L-13 (operator logins not in audit_log), api-grpc 9104 on the VNet, Traefik 8080 mapped, the b1a5915 decision (owner), Loki (owner's peer). Step 4: install.sh on Linux x86_64 in a fresh HOME, four archives match checksums.txt, arm64 binary runs under qemu-user, macOS archives are valid Mach-O; the three other-machine installs and a tag with --version are owed. CHECKLIST rows ticked with evidence: benchmark/I-12, isolation, privacy and terms, runbook entries per alert; fragment row four of five ((c) 31 minutes held by the conductor until m3's kernel sweep leaves host-01). Test project repose-m5-frag (small, owner's login) alive for the 31-minute row, destroyed after. Steps 2 and 3 wait on the conductor (second host, second human).
2026-09-21 | m5-release | ws/m5-release session (Fable 5.1) | progress (writeup) | Merged to main through 270dd12: I-136 (public /metrics, 404 live since 02:30Z), I-137 (wg0.conf writer), I-138 (--name sync), I-139 (Host CA in RegisterResponse and Rotate; hostd re-renders after a rotate), I-140 (operator logins to audit_log with key id and serial), repose --version, prerendered legal pages; M-4 removed from the machines at 02:20Z; M-2 verified closed on the live edge. docs/security/review-2026-09-21.md 'At a glance' has the closed and open lists with owners and dates. Host switch carrying I-137/I-139/I-140 (plus the runbook host_ca_pub step) is the conductor's, after m3's kernel row. Full go test ./... green except one billing test that timed out pinging its temp Postgres under the parallel load and passes alone; nix flake check and nix build of hostd and api pass at 270dd12. Still open for this session: the 31-minute fragment row (waiting for '31 go'), then destroy repose-m5-frag and the final report.
