# Workstream status

One line per claim. Newest at the bottom. Format:
`YYYY-MM-DD | <workstream> | <who> | claimed|progress|done|blocked | <note>`

One line per entry, at most 600 characters. Details go in the commit
message, `docs/RESEARCH.md` or the workstream doc, and the line points at
them. A 5,000-character line is read by every session that opens this
file and is the reason it had to be split.

This file keeps the newest line per workstream and the last fifteen lines.
Older lines, and the full text of any kept line that was shortened, are in
[status-archive/2026-09.md](status-archive/2026-09.md) verbatim; when this
file grows past about 60 lines, move the older ones to the archive for the
month the line is dated in.

2026-09-17 | docs | interview session | done | design, decisions, interfaces, workstreams written
2026-09-17 | scaffold | interview session | done | go.mod, cmd stubs, proto contracts, buf config, nix flake skeleton (flake check passes), infra skeleton, justfile, AGENTS.md
2026-09-17 | 00-benchmark | deferred | | owner skipped it (DECISIONS I-12); first M1 host records timings instead
2026-09-19 | 01-host-nixos | ws/01-host-nixos agent (Fable 5.1) | done (local) | nix/hosts complete: nixosConfigurations.host and host-bench build, nix flake check passes with 3 VM tests (network isolation, thin pool, services/registration/ssh audit); host-conventions.md rewritten, DECISIONS I-18, runbook entries. Not done: every real-host item (nixos-anywhere, lsblk on Azure, Grafana view, ss on the Azure NIC) and CI (no CI exists yet); buf lint fails on the untouched proto scaffold. hostd is the stub until 03 merges packages.hostd.
2026-09-19 | 11-infra-opentofu | ws/011-infra-opentofu-host session | done | Owner reviewed and set the shape: nothing applied from this branch; the apply happens from main after 01 merges, so the host installs from the merged flake. Changes since the progress line: coolify_count defaults to 0 so the control-plane VM waits for wave 3 (I-24); the installer reaches hosts through the edge, never a temporary public IP, documented and decided (I-24); … (full line in status-archive/2026-09.md)
2026-09-19 | 03-hostd | ws/03-hostd-hostdev agent (Fable 5.1) | progress | done without a host: cmd/hostd (register, mTLS stream with reconnect/replay/sample buffering, every command against LVM/nft/tc/systemd-run/CH API/virtiofsd/nix behind interfaces with fakes, bbolt state, reconcile, GC roots, samples, console capture, metrics, control socket, operator subcommands), cmd/hostdev (I-17), internal/fakes/{hostd,guestd}, internal/vsockrpc, proto I-26 fields, nix packages hostd/hostdev, decisions I-26..I-20, nix-build-contract.md, runbook rows. … (full line in status-archive/2026-09.md)
2026-09-19 | 04-guestd | opus-5 session (ws/04-guestd) | done | cmd/guestd (+ `call` client), cmd/repose-hook, internal/vsockrpc, internal/guestd/*, internal/fakes/guestd, nix packages.guestd and the NixOS VM test. `nix flake check ./nix` green, `go test -race` green, lint clean. Interfaces: vsock-guestd.md; DECISIONS I-29..I-21. NOT done: the real-guest item (needs a host and hostd, workstream 03); `mkGuest`/guest base installation of the unit is 02's.
2026-09-19 | 02-guest-base | claude session (ws/guest-base worktree) | done | nix/guest/base (17 modules), nix/overlay/agents wrappers+hooks+MCP, mkGuestRunner (bin/run with run-time guest args, I-34), VM tests guest-base/guest-docker/guest-desktop + closure check pass, runner booted under Cloud Hypervisor on the dev box with SSH-cert login, docker, desktop; packages/core retired. Not done: hooks.sock end-to-end and hostd AgentEvent (needs 04 guestd), IMDS/guest-to-guest blocking (host nftables, 01/03), CI wiring; gemini-cli is marked for removal in nixpkgs (12 decides).
2026-09-19 | merge wave 1 | interview session | done | 01, 11, 03, 04, 02 merged into main (2eed4f7): flake check, go build/vet/test, nix builds of guestd/hostd/hostdev/repose-hook all pass; decisions renumbered I-18..I-38; 03/04 vsockrpc duplication kept as I-37; ci.yml added; prod plan clean (28 resources, hosts=[]). Apply blocked for the owner to run (permission), see PROMPTS.md "M1 bring-up".
2026-09-20 | 10-observability | ws/10-observability session (Opus 5) | progress | Five bugs in merged code, found by building the enforcement rather than by reading: repose_host_guestd_unreachable carried the guest_id label §5 forbids; hostd logged an operator's exec argv; guestd emitted thaw and freeze_timeout through a variable so no grep found them; obs.SetupTracing failed for every caller on a semconv schema conflict, which means tracing had never been switched on; … (full line in status-archive/2026-09.md)
2026-09-20 | 05-control-plane-api | ws/05-control-plane-api agent (Fable 5.1) | done (local) | cmd/api (API_MODE http|grpc|all, migrations, graceful drain), cmd/repose-admin (full I-9 surface), internal/api/{http,auth,hostmgr,ops,scheduler,ca,secrets,config,buildlog,events,meter,notify,snapshots,basebump,ratelimit,metrics,app}, internal/db (2 migrations, up/down, partitions, temp-Postgres test helper), internal/{obs,otel,billing,nixmenu,admin,ca/sshca,ca/testca,api/pki}, internal/fakes/{api,kv,blob,logto}, cmd/api/Dockerfile, ops/coolify/api.env.examp … (full line in status-archive/2026-09.md)
2026-09-20 | 12-nix-config-pipeline | ws/12-nix-config-pipeline agent (Fable 5.1) | done (local) | Done without a host: fragment contract as nix/guest/contract.nix (repose.overlays pre-pass, repose.system through nix/guest/system-allowlist.json, errors attributed to fragment.nix), nix/guest/compose.nix, flake inputs.fragment + guestSystem + lib.composeGuest + checks.fragment-examples/fragment-contract, three docs/features/config-examples that build; … (full line in status-archive/2026-09.md)
2026-09-20 | 07-cli | ws/07-cli agent (Sonnet 5) | done (local) | cmd/repose + internal/cli, every command and flag in 07-cli.md §5.1 (`repose --help` tree diffed against it) plus I-8's events/notify. Login: PKCE loopback and device code against Logto's real `/oidc/...` paths (I-73; matches internal/api/auth's own two call sites), tested against a hand-written fake OIDC server (internal/fakes/logto is server-side JWT verification, not a user login flow). Cert: ensureCert reuse/refresh/reissue with a clock, exactly-one-Include-line golden test. … (full line in status-archive/2026-09.md)
2026-09-20 | 08-dashboard | ws/08-dashboard agent (Sonnet 5) | done (local) | Note: built on branch ws/08-dashboard-b (a second worktree/branch), not ws/08-dashboard — a stray concurrent process was writing to the original worktree; the owner's conductor session confirmed ws/08-dashboard-b is the one to merge and will retire the original. Merged main (f48b946) in; no conflicts. Every 5.2 route built: Logto SPA auth (`auth.svelte.ts`, PKCE, $env/dynamic/public, ssr=false); … (full line in status-archive/2026-09.md)
2026-09-20 | 13-notifications | ws/13-notifications agent (Sonnet 5) | done (local) | Audited 04/05's already-built pipeline against this doc and closed what was missing rather than rebuilding what worked. Added: unsubscribe (`internal/api/notify/unsubscribe.go`, HMAC key auto-provisioned into the platform secrets like the CA material, `GET /v1/notify/unsubscribe`, email template now embeds the link, `internal/fakes/api` gained the route too); `notify.Subject` for platform-event subject lines (`billing_stopped`); … (full line in status-archive/2026-09.md)
2026-09-20 | 14-security-followup | ws/14-security-followup agent (Fable 5.1) | done (local) | H-2 fixed (DECISIONS I-51): guest@<id> runs Cloud Hypervisor as the hostd user with the sandbox pinned by internal/hostd/guest/testdata/unit.golden (NoNewPrivileges, no capabilities, ProtectSystem=strict, DevicePolicy=closed with /dev/kvm, /dev/net/tun and its own volume, private guests directory via TemporaryFileSystem+BindPaths, AF_UNIX/AF_VSOCK only), --seccomp true on the argv, hostd in kvm, udev rule making vg-guests/g-* group hostd, guest dirs 1770 roo … (full line in status-archive/2026-09.md)
2026-09-20 | m1-integration | ws/m1-integration session (Fable 5.1) | follow-ups | (a) nix/hosts/tests host-services: the repose-register assertion needs a fake api in the test node now that hostModules installs the real hostd (the test pins the stub for the moment); (b) the runtime nft rule that lets the host reach a guest for SSH before WireGuard (ops/RUNBOOK.md 'Reaching a guest before WireGuard exists') needs a permanent home in nix/hosts (01) or goes away with 06; … (full line in status-archive/2026-09.md)
2026-09-20 | 01/03-host-followup | ws/01-host-followup session (Opus 5) | done (local) | (a) I-74: `guest_in` starts with `ct direction reply ct state established,related accept`, so the host reaches a guest on 22 with no hand-inserted rule and the reachability survives `systemctl reload nftables`; host-network asserts nc to a guest's 22 with data arriving, ping, the same after a reload, and that a guest still cannot open 22 or 9100 to the host; … (full line in status-archive/2026-09.md)
2026-09-20 | 09-billing | ws/09-billing agent (Opus 5) | done (local) | internal/billing: the rollup moved out of internal/api/meter and extended with the billing period anchored at signup (Stripe's short-month clamping, period_start/period_end on every row), the class-change cap rule, the trial-credit debit inside the insert's transaction and the trial-to-active transition, the per-part Stripe push; internal/billing/{period,ledger,rollup,stripe,webhook,dunning,reconcile,explain,enforce,config,prices,ports}.go; … (full line in status-archive/2026-09.md)
2026-09-20 | 06-gateway-edge | ws/06-gateway-edge agent (Fable 5.1) | done (local) | Built without a staging edge: internal/gateway (SSH server + certificate auth chain per ssh-gateway.md; revocation/CA/route/gateway-cert caches with the §5.2 refresh and stale windows; terminate-and-redial relay with per-project 5-min gateway cert, session/direct-tcpip/forwarded-tcpip/agent relay, pty/window-change/env-filter/exit-status, keepalive, 24h cap; per-source auth limiter + fail2ban ban + connection cap; session reporting; wgsync; … (full line in status-archive/2026-09.md)
2026-09-20 | conductor (waves 1-3) | orchestration session (Fable 5.1) | done | Wave three merged: 06-gateway-edge landed last (72650ad); every ws/* branch and worktree removed. CI green on main at 30bcb6b after four post-merge fixes: nix src filter keeps test/fake-logto (vendor step), CLI tmux test uses remain-on-exit (runner has no claude), dashboard Start tests wait for the page before stopping, api create reads its row inside the transaction. Next: M2 (docs/MILESTONES.md), blocked on owner steps: Coolify first-run (0 users), the four DNS records (still on the proxied wildcard), Logto apps.
2026-09-20 | 11-infra-opentofu (control plane) | orchestration session (Fable 5.1) | done | Owner decisions: Logto is the existing accounts.herakraft.co (I-84; auth.repose record and Logto step removed; api/dashboard/CLI defaults point there), Coolify reaches the VM over Tailscale (I-86; cloud-init installs it, owner runs `tailscale up`; coolify_manager_cidrs kept as fallback), 80/443 open to the internet (applied). Found and fixed on the way: the api verified iss as the bare endpoint, real Logto sends <endpoint>/oidc (I-85). … (full line in status-archive/2026-09.md)
2026-09-20 | control-plane deploy | orchestration session (Fable 5.1) | done | Owner: Coolify overwrites container_name. Verified in Coolify's parser (bootstrap/helpers/parsers.php): container_name becomes <service>-<uuid> and network aliases are stripped; Compose's own service-name alias survives, so the service is now named repose-postgres and the Postgres service gets "Connect to predefined network"; apps are on the coolify network by default. Docs and env examples follow.
2026-09-20 | 07-cli (release) | orchestration session (Fable 5.1) | done | I-98: install.sh points at Heracraft/factory, release.yml publishes on v* tags, the web build serves /install.sh; v0.1.0 tagged. Gate step 1 is the documented curl | sh once the web image is redeployed from main.
2026-09-20 | 14-security | ws/m3-integration session (Fable 5.1) | review 07 | credential sync reviewed: an allowlist of exactly the four guest-conventions rows over the user's own SSH session as a one-file tar with mode 0600 (0644 for .gitconfig), never Claude, Gemini or SSH keys (pinned by run_integration_test.go), the CLI's own refresh token 0600 or in the keychain. One finding, L-12 (Low, 07): the mtime rule of features/secrets.md is not implemented; a login done inside the guest is overwritten by the next `repose run`. … (full line in status-archive/2026-09.md)
2026-09-20 | m1-integration follow-up (a) | ws/m3-integration session (Fable 5.1) | done | nix/hosts/tests host-services registers the real hostd against a real hostdev in a second node (01/03 follow-up, I-75, I-76); verified on this box's KVM 2026-09-20 18:11Z: `nix build ./nix#checks.x86_64-linux.host-services` exit 0, 21 subtests, hostdev logged `host registered host_id=host-01a0c004`, hostd `registered`, `connected: true`, token consumed once and a second token refused, `hostd status` on the control socket, nightly snapshot unit ran, operator logi … (full line in status-archive/2026-09.md)
2026-09-20 | 07-cli (gate fix) | orchestration session (Fable 5.1) | done | I-102: device-code and refresh requests now carry resource=api; the api had refused the opaque token. v0.1.3.
2026-09-20 | m2 e2e from the dev box | orchestration session (Fable 5.1) | done | CLI proven end to end against production from a build of main: device login (I-99, I-101, I-102), notify set/test delivered to ntfy, create with the CLI waiting on the create op (I-106), build + boot on host-01 (~1 min), sync over an SSH origin through the forwarded agent (I-107, I-110 on the switched edge; I-111 stood in per-repo), stop with snapshot (22 s), start, `ssh <slug>.repose` (I-108), gateway sessions counted, tz (I-104), guest ssh include (I-109). … (full line in status-archive/2026-09.md)
2026-09-21 | 07-cli (finding) | ws/m3-integration session (Fable 5.1) | open | The CLI has no re-issue path on a revoked certificate: ensureCert reuses the on-disk certificate while it has validity left, and a gateway refusal ("certificate revoked") ends `repose run` at the SSH step (seen on this box after test/isolation's revocation row, 2026-09-21 00:16Z). 07 §9's "gateway rejects certificate, retry once" row: the refusal should trigger one POST /certs re-issue, then fail. Recovery today: `rm ~/.ssh/repose/id_ed25519-cert.pub && repose run`. Conductor's call: fix later, not tonight.
2026-09-21 | 05/12 (finding) | ws/m3-integration session (Fable 5.1) | open | `repose-admin base publish --rev` accepts any string: a short or mistyped sha (2026.09.21 was published as `3f83664f`, which does not exist) reaches hostd, whose clone fails with `pathspec did not match`, and every create on that base fails until the next publish. It should resolve a short sha through the platform repository or refuse it, and refuse a rev the repository does not have (the M2 finding again). The row 2026.09.21 stays in base_versions, superseded by 2026.09.21.1 (full sha).
2026-09-21 | m2-integration | ws/m2-integration session (Fable 5.1) | gate done | M2 closed on the owner's call at 01:02Z with one person plus a second account (DECISIONS I-129; MILESTONES M2 gate annotated): the owner's laptop run through the released CLI v0.1.4 (device-code login on user-c7fh26yzrl93, nuru-playground and age-calculator created, built and running on host-01, relays from the laptop through the switched edge), m3's isolation suite under two accounts (18 rows pass), and the ntfy deliveries. … (full line in status-archive/2026-09.md)
2026-09-21 | m3-integration | ws/m3-integration session (Fable 5.1) | stopped | M3 guests half done through the real api on host-01; every 05/12/13/14 row this session owned is closed with evidence (05 expiry with real blob deletes; 12 kernel_changed with both Build results; 13 five agents; 14 boundary rows). … (full line in status-archive/2026-09.md)
2026-09-21 | 11-infra-opentofu (conductor) | conductor session | needs apply | I-131: the api's service principal gets Storage Blob Data Contributor on the snapshots container (`modules/storage` `api_identity_object_id`, passed by `modules/environment`), after the first production expiry run was refused with 403 AuthorizationPermissionMismatch at 01:27Z. `tofu validate` and `fmt` clean; the owner applies from infra/azure/prod; the 05 expiry row stays open until the rerun after the apply.
2026-09-21 | m5-release | ws/m5-release session (Fable 5.1) | done (steps 1 and 4 as far as one host and no second human allow) | Merged to main through 270dd12 plus docs at bf31776: the deployed-state review (docs/security/review-2026-09-21.md, 'At a glance' for the closed and open lists with owners and dates), I-136 public /metrics (404 live), I-137 wg0.conf writer, I-138 --name sync, I-139 Host CA in RegisterResponse, I-140 operator logins to audit_log, repose --version, prerendered legal pages (passages verified live 02:47Z); … (full line in status-archive/2026-09.md)
2026-09-21 | m3-web | ws/m3-web session (Opus 5) | progress | host-01's Fluent Bit settled, read-only from the host by the conductor: `inactive (dead) (Result: exec-condition)` since 00:16:44Z, ExecCondition exited 1, `LOKI_HOST=` present and empty. So it is I-95 meeting a host with no Loki recorded and nothing else broke — and the journal's useful half is that it had **run for 6h 1min before the switch, retrying an empty target and shipping nothing**, which is precisely the state I-95 converts into a refusal with a reason and `FluentBitLogShipperDown … (full line in status-archive/2026-09.md)
2026-09-23 | server-robust | worker session (Opus 5.5) | done (branch, not merged) | I-156..I-159, api-side only (hostd unchanged, no host switch needed): ops.recoverFrom replans a destroy/stop whose freeze hit a dead guestd as stop-then-snapshot-of-the-stopped-volume, skips an impossible destroy snapshot with snapshot_failed, treats not_found in a destroy as done, and turns a start whose ApplyConfig lost guestd into a restart; … (full line in status-archive/2026-09.md)
2026-09-23 | provision-speed | provision-speed worker (Opus 5.5) | stopped 02:40Z | Done on branch worktree-agent-a63f575b79c05793e, nothing live changed. I-160 (api: create reuses a closure the host already runs, no Build), I-163 (api: NOTIFY wakes the ops driver across api/api-grpc) deploy on push. I-161 (base: guestd/sshd not after docker, 0.1 s ready/paths polls, console tty params, no initrd credential mounts; dev box Ready 11.88 -> 6.3 s) needs a base publish. I-162 (hostd: mkfs lazy_itable_init=1, ~1 s off create) needs a host switch. … (full line in status-archive/2026-09.md)
2026-09-23 | 07-cli (cli-ux) | cli-ux worker (Opus 5.5) | stopped (branch ready to merge, not released) | Done on the branch, rebased on server-robust's 29286eb: I-149 (own passphrase-less key ~/.ssh/repose/id_ed25519, cert re-issued off the user's key, ControlMaster multiplexing, one re-issue on Permission denied), I-150 (git-bundle sync, no guest fetch, no push prompt, empty guest gets full history, diverged guest branch left alone, --name projects same path superseding I-138; … (full line in status-archive/2026-09.md)
2026-09-23 | destroy-restore | destroy-restore worker (Opus 5.5) | stopped 03:10Z (branch ready to merge, nothing live changed) | The 33 s was dd|zstd reading the whole thin volume (host-01 journal: every 20 GB snapshot 15-16.5 s, every 40 GB one 31-35 s, 1.5 MB or 227 MB alike). I-164 (hostd, needs a host switch): dumpe2fs + extent stream of used non-zero blocks, raw fallback for needs_recovery/not-ext4, restore reads both; dev box 40 GB/1 GB file raw 23.6 s vs extents 0.23 s. … (full line in status-archive/2026-09.md)
2026-09-23 | m4-billing | m4-billing worker (Opus 5.5) | stopped (branch ready to merge, nothing live changed) | M4 is now a paste plus commands: `ops/stripe/bootstrap.sh` (repose-admin billing stripe-bootstrap, I-180) makes product, 3 meters, 3 prices, portal config and webhook endpoint from sk_test alone and prints the api's STRIPE_* block; `docs/ops/M4-GATE.md` is the runbook; … (full line in status-archive/2026-09.md)
2026-09-23 | hardening | hardening worker (Opus 5.5) | claimed | running-guest snapshot extents, `repose restore` from the remote, base publish --rev check, 9104/8080/bootstrap key, re-issue on revoked cert, gateway_sessions key (I-171..I-178)
2026-09-23 | hardening | hardening worker (Opus 5.5) | stopped (branch worktree-agent-a76ef0f8710c62c1d ready to merge, nothing live changed) | I-171: running-guest snapshots already take the extent path (journalled ext4 stays clean while mounted; freeze clears needs_recovery); host-01 03:08Z running-guest snapshots 763 ms and 1314 ms; real fsfreeze round-trip test. I-172 `repose restore` with no NAME by remote. I-173 base publish needs a full sha on main (GitHub compare). … (full line in status-archive/2026-09.md)
2026-09-23 | live-polish | live-polish worker (Opus 5.5) | claimed | conductor's live bugs of 04:03Z: stop hang with an open session, CLI polling vs rate limit, cert after restore, gateway refusal text, restore of a destroying project, projects --destroyed duplicates, doubled non-TTY progress line, status host name and stale event; DECISIONS I-186..I-192
2026-09-23 | live-polish | live-polish worker (Opus 5.5) | stopped 04:50Z (branch ready to merge, nothing live switched) | I-186 stop hang root-caused and reproduced live (full-sync run into e2e-polish, stop -> 1m00s; CH serial-manager thread vanished the second the stop began): hostd closed console capture with bytes unread -> ECONNRESET -> CH 53 serial thread exits without detaching -> guest UART never gets THRE -> PID 1 blocks on the console. … (full line in status-archive/2026-09.md)
2026-09-23 | conductor | conductor session (Opus 5.5) | progress 05:10Z | Merged hardening (I-171..I-177), m4-billing readiness (I-179..I-185; M4 waits only on the owner's sk_test key, runbook docs/ops/M4-GATE.md), live-polish (I-186..I-192: stop hang was hostd closing the console socket mid-output and cloud-hypervisor's serial thread dying), I-193 (b1a5915 rotated), I-194 (untracked sync vs pnpm symlinks). host-01 and edge switched to main (edge when no gateway connections were open). CLI v0.1.7 and v0.1.8 released. … (full line in status-archive/2026-09.md)
2026-09-23 | 15-dev-ergonomics | ws/15-dev-ergonomics session (Opus 5.5) | claimed | Workstream 15 §2 in order (I-195..I-205): TZ on every run/attach, git config carry minus denylist, Claude config carry with the jq settings.json merge, .env carry, auto-forward on the ControlMaster shown in tmux, repose cp, guestd-owned oom_score_adj, hybrid first sync, npm and Docker Hub caches per host, one-day trial, catalog CLIs plus vercel's login. Tested against the local sshd harness, the fake api and guest NixOS tests; … (full line in status-archive/2026-09.md)
2026-09-23 | 15-dev-ergonomics | ws/15-dev-ergonomics session (Opus 5.5) | stopped (branch ready for the conductor's review, rebased on origin/main 98d3304; nothing live touched) | All of §2 built, one commit series per part: I-198 TZ (carry part + PATCH /projects/:id tz + tmux update-environment without TZ, base), I-195 git carry (denylist, include-first precedence, identity migration), I-196 Claude carry (allowlist, jq merge with goldens, plugins in the background), I-197 .env over the sync's apply ssh, I-199 auto-forward (session helper `repose __s … (full line in status-archive/2026-09.md)
2026-09-23 | 15-dev-ergonomics (fixes) | ws15-fixes worker (Opus 5.5) | claimed | the conductor's review of ws/15 (items 1-12) and the second-run 'dirty tree' refusal; branch ws/15-fixes off ws/15-dev-ergonomics d0abcc0; nothing live
2026-09-23 | 15-dev-ergonomics (fixes) | ws15-fixes worker (Opus 5.5) | stopped (branch ws/15-fixes ready for the conductor, not pushed, nothing live) | Blockers: settings.json leaves the laptop without env/apiKeyHelper/aws*/gcp*/otelHeadersHelper/forceLogin*; git denylist gains extraHeader, cookieFile, smtpPass, github.token, hub.oauthtoken, gitProxy, *.proxy and any *token/*pass/*password/*secret (I-211). I-210: a guest tree equal to the last sync's fingerprint (.git/repose-synced, HEAD + git add -A tree) is not dirty; … (full line in status-archive/2026-09.md)
2026-09-23 | 15-dev-ergonomics (fixes) | ws15-fixes worker (Opus 5.5) | stopped again (second review done, branch ws/15-fixes at the next commit, not pushed, nothing live) | Second review: I-210 fingerprint fails on any submodule change and uses a throwaway object dir; every sync status/stash/reset forces status.showUntrackedFiles=normal and submodule.recurse=false (both keys stay carried); the last sync's changes are stashed ('repose run: last sync', said in the summary) instead of reset; … (full line in status-archive/2026-09.md)
2026-09-23 | 15-dev-ergonomics (fixes) | ws15-fixes worker (Opus 5.5) | stopped (third review done, branch ws/15-fixes, not pushed, nothing live) | Stash cap (newest 10 'repose run: last sync', sha-verified prune), clean-tree re-check before the tar, LFS-tolerant fingerprint, cp -p index copy, Bearer needs 20+ token chars, SSH-identity names only and skipped Claude files named once per change, git note lists sections, I-210 names git stash -u's record-then-clean gap.
2026-09-23 | 15-dev-ergonomics (live fixes) | ws15-fixes worker (Opus 5.5) | stopped (branch ws/15-live-fixes off main 7ecfb9b, not pushed, nothing live) | I-212 gateway: exit status relayed before EOF on a session, guest channel keepalive answered (real OpenSSH over a ControlMaster: exit 255 before, 7 after). I-213 guestd: agents found by .X-wrapped names, oom warning names the victim. I-214 hosts: npm cache ignores Set-Cookie (host-caches: MISS before, HIT after), docker-registry OTEL off. … (full line in status-archive/2026-09.md)
2026-09-23 | conductor | conductor session (Opus 5.5) | progress 10:00Z | ws15 (laptop parity, I-195..I-215) merged after three independent reviews and a fix worker (secrets in settings.json values and git config, I-210 dirty-tree with submodule/stash safety, I-211) and a live checklist on real guests (TZ, git/Claude carry with fake secrets absent, .env, forward, cp, OOM, caches, hybrid, timing +19 ms run/+12 ms attach p50). … (full line in status-archive/2026-09.md)
2026-09-23 | conductor | conductor session (Opus 5.5) | progress 10:45Z | CI soak flake root-caused (harness 3 s guest dial timeout under -race gave the gateway's not-ready 255; also a handshake deadline race, now a timer) in ab517be; edge switched with it; CLI v0.1.9 released (tag on ab517be) and installs cleanly. ws15 complete and live end to end.
2026-09-23 | upkeep | upkeep worker (Opus 5.5) | claimed | docs only: tick §9 checklist rows whose evidence is already recorded (cite it), mark the rest open/waits, docs/workstreams/CHECKLIST-AUDIT.md; archive old STATUS lines; generated DECISIONS-INDEX.md
2026-09-23 | upkeep | upkeep worker (Opus 5.5) | stopped (branch worktree-agent-ae268b82a7a5e1195, not pushed, nothing live) | 81 §9 rows ticked with citations, 75 open (evidence missing), 22 wait on the owner, 2 on a second host, 8 superseded (00, I-12); table and how-to-close list in workstreams/CHECKLIST-AUDIT.md. STATUS split to the newest line per workstream plus the last 15 (archive in status-archive/2026-09.md); DECISIONS-INDEX.md generated by ops/dev/decisions-index.py (--check).
2026-09-23 | 16-guest-tooling (conductor-run) | conductor session (Opus 5.5) + workers | claimed | owner's nuru-playground session: missing gcc/air/portless/tsc and slow pnpm installs. Workers: (a) npm cache gzip + tap shaping direction (I-217), (b) base toolchain, CLI tools, nix-ld, pinned nixpkgs registry, command-not-found (I-218/I-219), (c) repose config add takes any nixpkgs package (I-220), (d) laptop global-tools carry and project scan (I-221/I-222), (e) repose run startup profile and cuts (I-223+). Merged to main by the conductor, then deployed and tested end to end.
2026-09-23 | 16-guest-tooling (c) menu packages | menu-packages worker (Opus 5.5) | claimed | repose config add/remove take any nixpkgs attribute next to catalog ids (menu selection gains {package}), api/fake/CLI/dashboard, I-220
2026-09-23 | 16-guest-tooling (c) menu packages | menu-packages worker (Opus 5.5) | stopped (branch worktree-agent-aec69fd8963e7b549, not pushed, nothing live) | I-220 done: MenuSelection items may be {package: "<nixpkgs attr path>"} (strict pattern, rendered as string list through lib.attrByPath, missing -> 'nixpkgs has no package "x"; search https://search.nixos.org/packages'), empty selection allowed; repose config add/remove; fake api now uses internal/menu (the dashboard and fake spoke {packages,services,options}, which the real api never accepted); dashboard menu tab reads/writes the real array and lists Extra packages. Needs api redeploy + CLI release; no host switch, no base publish. Not done: ops/checks/menu.sh has no live {package} row.
2026-09-23 | 16-guest-tooling (a: cache speed) | host-net worker (Opus 5.5) | stopped (branch worktree-agent-a4ab8a0910200c11e, not pushed, nothing live) | I-217: hostd's 200 Mbit/s shape moved from an HTB on the tap root (which capped downloads into the guest, caches included, at 25 MB/s) to a policer on the tap's ingress (guest egress), with 10.64.0.0/12 and 10.63.255.254 exempt; reconcile re-applies it to running guests and removes the old root HTB (host-network at a 20 Mbit/s cap: legacy host->guest 19 Mbit/s; new host->guest 1191, guest->cache 1301, guest->internet 21). npm front gzips package documents (host-caches: Content-Encoding gzip, rewritten URLs, tarballs untouched). Docker mirror needed nothing beyond the shape. Needs a host-01 switch (kernel modules, nginx, hostd); running guests are re-shaped when the new hostd starts. Not done: a live measurement on host-01.
2026-09-23 | 16-guest-tooling (b) | base-tooling worker (Opus 5.5) | stopped (branch worktree-agent-a3c03e3ee97f7cc41, not pushed, nothing live) | I-218: gcc/binutils/make/pkg-config/cmake + file, lsof, zip, dnsutils, sqlite, psql-only, openssl, gnupg, psmisc in tool-list.nix; nix-ld with a common + chromium library set; nixpkgs registry/NIX_PATH pinned to the base's nixpkgs, global registry off; noVNC trimmed (no python3.14, no numpy/openblas) to pay for it: closure 6,017,269,544 -> 6,165,108,296 bytes. I-219: nix-index-database input (small db), repose-command-not-found for bash/zsh/fish with nix profile add / repose config add hints and /run/user/<uid>/repose-installing hook. New VM test guest-devtools passes; guest-base, guest-desktop, guest-closure-size pass. Not done: nothing tested on a real guest; ~/go/bin on PATH is the conductor's follow-up.
2026-09-23 | 16-guest-tooling (tools carry) | tools-carry worker (Opus 5.5) | claimed | laptop->guest tool parity: global tools carry (I-221) and project scan with `repose scan` (I-222); CLI carry_tools*/scan*, guest nix/guest/base/tools-carry.nix, guest VM test; nothing live
2026-09-23 | 16-guest-tooling (tools carry) | tools-carry worker (Opus 5.5) | stopped (branch worktree-agent-ac1242b9126a9b535, not pushed, nothing live) | I-221 global tools carry (npm/pnpm/bun/go buildinfo/cargo/uv/pipx read from disk, marker `tools`, ~0.5 ms laptop side, overlaps the probe ssh) and I-222 project scan + `repose scan [DIR]`; guest repose-tools-install plan/run + user unit repose-tools-carry (nix profile add via nix-locate first, managers into the login PATH, notices once, node major into the profile only when it wins). guest-tools-carry VM test passes offline (fake nixpkgs flake, fake nix-locate, local npm registry, reboot resume). Not exercised: real go/cargo/uv installs (network), real nix-locate index (other worker). Needs base publish + CLI release.
2026-09-23 | 16-guest-tooling (e: startup) | startup worker (Opus 5.5) | stopped (branch worktree-agent-adb5195c03d860eac, not pushed, nothing live changed) | I-223 CLI: REPOSE_TIMING=1 per-phase lines, no second project read, connect fast path (files cover + live master: no api, no `ssh true`), probe started beside the api (warm and cold), `attach` with no api call, 500 ms op polls to 30 s, pre-start guestd samples ignored. I-224 CLI: logins+carry ride the apply ssh, apply skipped when the guest already has this sync (`.git/repose-synced-key`), logins marker `creds` + `~/.repose/creds-paths`. I-225 server: hostd dials a booting guestd every 200 ms (host switch), guestd skips an already-loaded path registration (base publish), api ignores samples older than started_at (api redeploy). Live medians at 200 ms RTT (ops/dev/startup-bench.sh -r 200): warm run 4509 -> 1197 ms, attach 2166 -> 470, cold master 6011 -> 2697, dirty 4346 -> 2195, stopped 16235 -> 14473. Not done: boot itself (initrd/switch-root, home-manager 1.35 s), the 5.4 s no-op build on create with no running guest of the closure, ControlPersist length (interface; conductor's call). e2e-startup* projects all destroyed (snapshots kept 30 days, no CLI purge).
2026-09-23 | 16-guest-tooling (compat) | compat worker (Opus 5.5) | claimed | prebuilt-binary ecosystem tools in the guest out of the box (prisma platform, playwright/puppeteer, rustup, uv pythons, node natives, go/cli downloads); nix/guest/base/compat.nix + nix-ld libs; I-228/I-229
2026-09-23 | 16-guest-tooling (compat) | compat worker (Opus 5.5) | stopped (branch worktree-agent-a4df46c34ceb77c2a, not pushed, nothing live) | I-228 nix/guest/base/compat.nix: Prisma engines redirect on 127.0.0.1:850 + PRISMA_ENGINES_MIRROR (linux-nixos -> debian-openssl-3.0.x, any version; prisma 6.16 and 5.22 generate/migrate dev/query live), PLAYWRIGHT_BROWSERS_PATH=~/.cache/ms-playwright seeded from the packaged browsers (install used to hang 10 min on the read-only store), system python3 wrapper on nix-ld libs (manylinux numpy under python3 -m venv / uv venv), PKG_CONFIG_PATH for openssl/zlib/sqlite/libffi (openssl-sys). Audit of ~40 other tools needed nothing. VM test guest-compat, guest-desktop, guest-closure-size pass (+8,872 bytes). Needs a base publish. Not done: pyenv not checked; Prisma's nixos warning still printed (harmless).
2026-09-23 | 16-guest-tooling (b2) | base-tooling worker (Opus 5.5) | stopped (same branch, commit after b5160a6, not pushed, nothing live) | I-227: 21 package-manager user bin dirs (nix/guest/base/user-bin-dirs.nix) first on PATH via environment.sessionVariables (set-environment + PAM: ssh commands, user manager); GOPATH/CARGO_HOME/RUSTUP_HOME/BUN_INSTALL/DENO_INSTALL_ROOT/COMPOSER_HOME/GEM_HOME set; tmux server global PATH set from a login shell by repose-tmux-session; activation refreshes a running tmux server and user manager; repose-tmux-session no longer dies without /etc/repose/env. guest-devtools, guest-base, guest-parity, guest-closure-size pass. Not covered: nvm/fnm/sdkman/pyenv shims, gem --user-install, corepack enable (documented in I-227).
2026-09-23 | 16-guest-tooling (guest memory) | guest-memory worker (Opus 5.5) | claimed | host-01 e2e-tools large guest memcg oom-kill at 20:16:59Z: guest disk direct I/O, virtiofsd cache/limits, unit overhead/MemoryHigh/OOM policy, guests.slice + capacity math; I-230
2026-09-23 | 16-guest-tooling (guest memory) | guest-memory worker (Opus 5.5) | stopped (branch worktree-agent-a13f7e6ebd7fe71fe, not pushed, nothing live) | I-230: host-01 e2e-tools OOM was CH's buffered disk writes (460 MB under writeback) charged to guest@ on top of the shmem RAM. CH --disk gains direct=on (ch.go, runner), guest@ gets MemoryHigh=class+384M, OverheadMiB stays 512 (capacity math unchanged), virtiofsd unchanged (read-only, own unit). Repro script check host-guest-memory (a VM test nests CH three levels and never booted here): before 612 MiB host cache and 7353 memory.max hits, after 25 MiB and 0; throughput kept (4k QD1 -15 percent). Needs a host switch; applies per guest at next start. Not done: 4096-byte-sector path verified only by CH code, check live after first restart.
2026-09-23 | 16-guest-tooling (conductor-run) | conductor session (Opus 5.5) | stopped 23:40Z | Live: host-01 switched (I-217 egress-only shaping + npm gzip; I-225 hostd boot dial; I-230 direct=on disks + MemoryHigh), base 2026.09.23.4 (I-218/219 toolchain, nix-ld, pinned registry, command-not-found; I-221/222 tools carry + scan installer; I-227 every package manager bin dir on PATH; I-228 Prisma/Playwright/python wheels/openssl-sys), api (I-220 config add any nixpkgs package, I-226 request log route/user/client, guest error reason), CLI v0.1.10 (config add/remove, repose scan, tools carry, I-223/224 fewer round trips, REPOSE_TIMING). E2E on fresh e2e-nuru (pnpm+turbo+portless+air/cgo+prisma+node 22): tools arrived ~10 s after Ready (nixpkgs air/buf/golangci-lint/protoc-gen-go, npm portless), node v22.23.2, cold pnpm install 13 s no slow-request warnings, prisma migrate dev ok, pnpm dev ran next+air (cgo says 42), go install bin on PATH in shells and user units, uv/venv numpy, playwright 1.55 install 13 s + launch, 8.4 GB writes peak 8244/8704 MiB no OOM. Bench at 200 ms RTT: warm 1.6 s, cold master 3.5 s, stopped 12.5 s, attach 1.1 s. Open: existing guests (the owner's) get direct=on only at their next stop/start; guest boot ~10 s is the next startup cut; ControlPersist left at 10m (dead-master hang after sleep).
2026-09-24 | 16-guest-tooling (boot time) | boot-time worker (Opus 5.5) | stopped (branch worktree-agent-a136ba2e2d4d673fc, not pushed, nothing live) | I-231 guest boot: scripted stage 1, stage-2 credential/API-mount rate-limit stall gone, zram via repose-zram-swap (off swap.target), setuid wrappers before logind + restored from /var/lib/repose/wrappers, no BPF LSM, home-manager skipped for an unchanged generation, repose-npm-registry out of default.target, guestd SetupProject one systemctl start; dev box CH->running 9.09 -> 5.71 s median (live expected StartGuest->running 8.3 -> ~5.2 s). I-232 hostd: boot dial 50 ms, virtiofsd socket 10 ms, dump-db read during boot. I-233 memory snapshot/restore measured (0.29 s snapshot, 0.5-1.2 s restore), not adopted (secrets on disk, clock, virtiofsd state). VM tests + host-services pass. Not done: live numbers (needs base publish + host switch), userborn/etc-overlay for activation (0.4 s), dev's user manager start (0.66 s), CLI side (~4.5 s of RTT at 200 ms: poll, ssh handshake, sync, attach)
2026-09-24 | guest boot (conductor-run) | conductor session (Opus 5.5) + boot worker | stopped 02:10Z | Live: host-01 switched (I-233 hostd 50 ms dial, parallel start steps), bases 2026.09.24 (I-231/232 scripted initrd, fewer boot mounts, zram/wrappers off the path, home-manager skipped when unchanged) and 2026.09.24.1 (I-234: sshd reload retried during boot, pinning in the background at boot — both regressions found live). startup-bench -r 200 stopped on e2e-bootlive2: 8/8 ok, median 8.9 s (runs 2-8: 10.5, 9.06, 8.90, 8.92, 8.98, 8.95, 8.93; first stop after create 15.5 s), was 12.5-13.1 s; ensure-running median 5.37 s. Not done: CLI-side ~3.5 s at 200 ms RTT (op polling dead time ~0.45 s, ssh handshake without a master 1.9 s, two sync scripts); VM memory snapshot/resume is a proposal in I-233 (secrets on host disk, clock, virtiofsd state).
2026-09-24 | cli startup (op long-poll, ssh overlap) | cli-startup worker (Opus 5.5) | claimed | repose run/start on a stopped guest: op long-poll GET .../ops/{id}?wait (api+fake+CLI), master dial overlapped with the boot tail, sync scripts in one round trip, other overlaps; I-236/I-237; e2e-rt* projects only
2026-09-24 | cli startup (op long-poll, ssh overlap) | cli-startup worker (Opus 5.5) | stopped (branch worktree-agent-a51a8a5a6a2af6bb7, not pushed, nothing live changed) | I-236: GET /projects/:id/ops/:op_id?wait=&seen= long-poll (100 ms server re-read of one row, no held connection, 20 s cap, 4 held per user / 1000 total, answers at drain), op reads carry version/project_state/phase, fake api too (StartDelay, NoLongPoll); CLI holds reads back to back, falls back to polling with the project read beside the op read and 500 ms start-to-start, rides out 502/503/504 and dropped connections for 30 s; repose start drops its second project read. I-237: the first ssh after a start is the sync's probe, sent the moment the op ends beside the project read; stale masters closed first, a failed one never reused, early-probe refusals waited out of the gateway's 5 s route cache. Live at 200 ms against the current api (e2e-rt, stopped): v0.1.10 8988 ms median -> 8092 ms; remote-backed checkout 9009 -> 8596. Simulated op-end-to-first-ssh lag: 684-844 ms (v0.1.10) -> 296 ms (new CLI, current api) -> 157-167 ms (with the api deployed). Not done: gateway holding auth while a project is starting (about 1.4 s more at 200 ms, gateway lane), speculative probe+apply fold (rejected in I-237). Needs: api redeploy (push) and a CLI release. e2e-rt, e2e-rt2 destroyed.
2026-09-24 | CLI round trips (conductor-run) | conductor session (Opus 5.5) + worker | stopped | Live: api long-poll on op reads (I-236, seen held 4.4 s in a start), CLI v0.1.11 (I-237 first ssh at the op's end, no ssh true; fallback polling paced from request start; 502/504 retried through redeploys). startup-bench -r 200 stopped on e2e-rt3, 8 runs: median 8.58 s (8.51-9.51), was 8.9 s on v0.1.10; ensure-running median 5.53 s (host boot variance; the worker saw 5.13 s and 8.09 s total). Not done: probe+apply in one ssh (I-210 script risk), gateway holding auth while starting (~1.4 s at 200 ms, gateway lane). Memory snapshot options documented as I-235, nothing built.
2026-09-24 | user docs (/docs port + edit) | docs worker (Opus 5.5) | stopped (branch worktree-agent-a657a1e5c6d93f89a, not pushed, nothing live) | Ported e9c8979's /docs onto main and cut 21 pages to 14 (quickstart as /docs, install, run and attach incl. SSH/editors, sync, the machine incl. tools/ports/browser, config, secrets and security, agents, notifications, projects and lifecycle incl. status/logs, pricing, limits and acceptable use, CLI reference incl. config.toml, troubleshooting). Accuracy vs main: config add/remove (I-220), tool carry and repose scan (I-221/222), PATH dirs (I-227), base toolchain/nix-ld/command-not-found (I-218/219), Prisma/Playwright/wheels/openssl-sys (I-228), ~10 s start and REPOSE_TIMING (I-223..237); no card step; completion is bash|zsh|fish. Docs UI: section eyebrow and uppercase labels removed. Evidence: vitest 46 passed, svelte-check 0/0, eslint and prettier clean, pnpm build ok, playwright 39 passed (new tests/docs.spec.ts). Depends on unmerged work: open --desktop password/--stop, [sync] exclude, default_agent, secrets in agents started by run (CLI port worker); port 25/stratum blocks, miner stop, new-connection limit and terms acceptable use (I-238..I-240, abuse worker). Not done: homepage Docs link (conductor), project page Config/Secrets links from e9c8979 (other lane).
2026-09-24 | user-docs gaps port (I-241) | docs-gaps worker (Opus 5.5) | stopped (branch worktree-agent-a686a34e9793d2a09, not pushed, nothing live) | Ported 43b04a2 (web/landing-page, was I-227) onto main as I-241: open --desktop via repose-guest-profile + VNC password + --desktop --stop; [sync] exclude table; --agent exit 2 outside the five; default_agent sent as agent_default on create (fake api too); agent wrapper sources /etc/profile.d/repose.sh (REPOSE_PROJECT + named secrets; PATH already via I-227), asserted in guest-devtools; mcp forward/browser bridge name public docs; dashboard project page Config/Secrets links. Not done: CLI release, base publish (wrapper), web deploy; docs pages anchors live only once the /docs port lands.
2026-09-24 | anti-abuse (smtp, mining, floods) | abuse worker (Opus 5.5) | claimed | I-238 guest egress port 25 dropped + per-guest counters/metric/alert; I-239 stratum ports dropped, miner process names auto-stop (3 strikes then start refused until repose-admin abuse clear), CPU-without-anyone alert; I-240 per-guest new-flow rate limit measured on an e2e-abuse guest; terms acceptable use. Billing untouched; nothing deployed
2026-09-24 | anti-abuse (smtp, mining, floods) | abuse worker (Opus 5.5) | stopped (branch worktree-agent-a5ec82a17f4248d25, not pushed, nothing deployed) | I-238 tcp 25 dropped before guest_dyn (465/587 open), per-guest smtp/stratum/flows counters in hostd-owned chains, repose_host_egress_blocked_{total,guests}{reason}, egress_blocked log, EgressBlocked alert; AddGuestRules bug fixed (restarted guests had no egress rule) and reconcile re-applies rules. I-239 stratum ports 3333/5555/7777/14433/14444 dropped; internal/abuse miner names; api stops a guest naming one (snapshot stop op, abuse_events 0005, last_error abuse_stopped, abuse_stopped notification, MinerStopped alert), 3rd stop in 24 h holds start/restore (403 abuse_hold) until repose-admin abuse clear; CLI/dashboard show the reason; BusyUnattended alert (6 h full CPU, no ssh/tmux/agent); EgressHigh gauge now actually set. I-240 per-guest new-flow limit 200/s burst 2000, measured on e2e-abuse (peak: pnpm from npmjs 74/s, headless chromium 196/s in 1 s, 82/s over 10 s; destroyed). Terms acceptable use + privacy updated. host-network, host-services, Go, promtool, playwright pass. Needs: host-01 switch, api redeploy (migration 0005), base publish (guestd watch list), web redeploy, CLI release. Not done: nothing live-verified; renamed miners caught only by BusyUnattended/stratum
2026-09-24 | user docs audit + enforcement (I-242) | docs-audit worker (Opus 5.5) | stopped (branch worktree-agent-a2ffb03fe92cc6e7a, not pushed, nothing live) | Audited CLI tree, config.toml, env vars, exit codes, dashboard routes, guest behaviour and I-149..I-241 against /docs; table in docs/workstreams/DOCS-AUDIT.md. Fixed 6 wrong statements (snapshot retention, project limit after first invoice, pricing example, yarn cache, dashboard "same as status", secret name rules) and ~40 gaps across 11 pages. Code: `repose resize` unhidden, unused `gateway` config key removed, ExitInterrupted=130, env var registry userEnvVars. internal/cli/docs_test.go fails on any CLI command/flag/config key/env var/exit code missing from or left behind in cli.md. CHECKLIST.md and CLAUDE.md carry the rule. Needs: CLI release (resize visible), web redeploy (docs). Not done: dashboard pages/guest behaviour have no automated docs check (only the CLI surface is enforced).
2026-09-24 | web + docs + abuse (conductor-run) | conductor session (Opus 5.5) + workers | stopped 05:10Z | Live: homepage from web/landing-page cherry-picked (hero ending with notification/reattach kept, 7-block features wall cut to 3 items, no card wording); /docs 14 pages (from 21) checked against main; docs_test.go fails when a command/flag/key/env var/exit code lacks docs (I-242); CLI v0.1.13 (I-241 desktop VNC password, [sync] exclude, --agent check, default_agent, agents get secrets; resize visible); anti-abuse I-238..I-240 live and checked on e2e-abuse2: port 25 and pool 3333 blocked, 587/465/443 open, fake xmrig stopped in 25 s with snapshot and reason, third stop holds the project (start 403), abuse list/clear work. CI flake TestBuildAndApply fixed (awaitSession skips the failed session). Owner-side: load ops/alerts.yaml (EgressBlocked, MinerStopped, BusyUnattended, EgressHigh fix) and dashboards/abuse.json into the personal Prometheus/Grafana. web/landing-page branch fully absorbed; can be deleted.
2026-09-24 | agent guide in the guest (I-243) | agent-guide worker (Opus 5.5) | claimed | one source nix/guest/base/agent-guide.md installed as Claude Code managed memory + codex/opencode/gemini/pi equivalents, CI check against /docs, VM test; nothing live
2026-09-24 | agent guide in the guest (I-243) | agent-guide worker (Opus 5.5) | stopped (branch worktree-agent-a0757e0842624e0e1, not pushed, nothing live) | nix/guest/base/agent-guide.md rendered by agent-guide.nix (comments dropped, needs: lines only when the guest has the command) into /etc/claude-code/CLAUDE.md, /etc/codex/config.toml developer_instructions, /etc/opencode/opencode.json instructions, and gemini/pi extensions linked by repose-agent-setup; no user file edited. internal/cli/agent_guide_test.go (docs sections covered, refs exist, commands in guest, agent-guide.commands current); guest-agent-guide VM test passes: all five agents send the guide and the user's own file to a stand-in API, user files unchanged, links idempotent. Not done: repose-notify/repose-ask lines are marked needs: and appear only once worker (2) ships those commands (check their argument shape at merge); browser line is true headless or headed; git line does not mention agent forwarding. Needs a base publish to reach guests.
2026-09-24 | round-3 CLI/guest fixes (I-247..I-249) | r3 cli-fixes worker (Opus 5.5) | stopped (branch worktree-agent-a869ec7b63b4103ab, not pushed, nothing deployed) | I-247 no agent forwarding: CLI ForwardAgent no, gateway refuses auth-agent-req/channel, guest sshd AllowAgentForwarding no, gh insteadOf for github SSH URLs; I-248 run with nothing new on the laptop attaches without syncing, refusal rewritten; I-249 plain command-not-found. Go tests, lint, docs tests, web unit, guest-devtools VM green. Needs: CLI release, base publish, edge switch (gateway). Not run live.
2026-09-24 | agent questions (repose-notify, repose-ask) | ask worker (Opus 5.5) | claimed | I-244 repose-notify/repose-ask in the guest (repose-hook subcommands, guestd socket routes, Question notify / AnswerQuestion request, hostd AgentQuestion event / AnswerQuestion command); I-245 questions table, signed single-use reply links (ntfy actions, email), dashboard reply, `repose reply`/`repose questions`; e2e-r3-ask only
2026-09-24 | agent questions (repose-notify, repose-ask) | ask worker (Opus 5.5) | stopped (branch worktree-agent-ac4d63c4b28b951bb, commit cd45870, not pushed, nothing deployed) | I-244/I-245 built end to end: repose-hook notify/ask (+ symlinks), guestd /notify /ask routes + tmpfs question store, Question notify/AnswerQuestion request, hostd AgentQuestion event/AnswerQuestion command, api 0006_questions + questions worker + routes + signed reply links (ntfy JSON actions, email), fake api, CLI questions/reply, dashboard Questions card, docs. Tests at every layer + guestd VM subtest pass. Not done: live test on e2e-r3-ask (needs api redeploy with migration, host switch, base publish, CLI release, web deploy); repose status does not show questions (repose questions does)
2026-09-24 | browser on the desktop (I-246) | desktop-browser worker (Opus 5.5) | claimed | agent's browser headed on :99 so repose open --desktop shows it and the user can take over; browser.nix, desktop.nix, MCP wrappers, open --desktop, features/browser.md, /docs machine Browser; e2e-r3-desk only
2026-09-24 | browser on the desktop (I-246) | desktop-browser worker (Opus 5.5) | stopped (branch worktree-agent-a7b6e1341d4f42615, not pushed, nothing deployed) | One headed Chromium per guest on :99 (repose-browser.service, socket 127.0.0.1:9224 -> 9225, profile ~/.local/share/repose/browser, system repose-browser.slice 37.5%, OOMPolicy=continue, --disable-gpu); playwright-mcp --cdp-endpoint (default context; nixpkgs isolated override skipped) and chrome-devtools-mcp --browserUrl (--executablePath only when launching, --no-performance-crux) share it; old --headless entries migrated by repose-agent-setup; viewer wants the browser, --stop stops the viewer only, Xvfb/openbox StopWhenUnneeded, browser idle-stops after 30 min with no CDP and no viewer client; 1440x900, openbox maximises, noVNC resize=scale; CLI never auto-forwards 9224/9225. guest-desktop (MCP stdio client, X capture 94% magenta, same tab in both servers, SIGKILL reconnect, WebGL, idle), guest-base, guest-closure-size, go test/vet/lint pass. Live: v0.1.13 open --desktop works end to end on e2e-r3-desk (old base), same browser flags started by hand showed the agent's page through the forwarded noVNC; e2e-r3-desk destroyed. Needs: base publish, CLI release (forward exclusions), web redeploy (docs, homepage line).
