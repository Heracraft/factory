# Workstream status

One line per claim. Newest at the bottom. Format:
`YYYY-MM-DD | <workstream> | <who> | claimed|progress|done|blocked | <note>`

2026-09-17 | docs | interview session | done | design, decisions, interfaces, workstreams written
2026-09-17 | scaffold | interview session | done | go.mod, cmd stubs, proto contracts, buf config, nix flake skeleton (flake check passes), infra skeleton, justfile, AGENTS.md
2026-09-17 | 00-benchmark | deferred | | owner skipped it (DECISIONS I-12); first M1 host records timings instead
2026-09-19 | 11-infra-opentofu | ws/011-infra-opentofu-host session | claimed | infra/bootstrap, infra/azure (network, host, edge, coolify, storage, keyvault, dns), infra/r2, Makefile targets, README; tofu validate + plan clean
2026-09-19 | 11-infra-opentofu | ws/011-infra-opentofu-host session | progress | Written and verified without applying. Done: infra/bootstrap; infra/azure/modules (environment, network, host, edge, coolify, storage, keyvault, nixos_anywhere); infra/dns as a module so records are in the same apply as the addresses; infra/azure/{prod,staging} roots on the real backend; infra/r2; Makefile (plan/apply/validate/policy/check/bootstrap/force-unlock); tfsec policies proven against a fixture; CI workflow; README with bootstrap, add host, rotate token, drain/destroy, force-unlock and a cost table re-derived from the Azure price API on 2026-09-19 (~$1,040/mo pre-launch, over the $1,000 budget alert). `tofu plan` on prod is clean: 31 to add, 0 to change, 0 to destroy, no errors or warnings; 39 with one host. Five guards triggered deliberately, each with its documented message. Docs: DECISIONS I-18..I-22, host-conventions join-token row, workstream doc, three runbook entries. NOT done, all of it needing an apply the owner authorises: host registers, /dev/kvm on a real host, IMDS from a host, the prevent_destroy replacement attempt against real state, the Blob lifecycle deletion, Key Vault wrap/unwrap and the denied get, R2 backup and restore rehearsal, DNS resolution, and somebody following the README end to end. Also waiting on a human: blob soft delete on reposetfstate3912 (versioning is on, soft delete is off), the api Entra app registration and the R2 token (I-20), a CLOUDFLARE_API_TOKEN before manage_dns can be true.
2026-09-19 | 11-infra-opentofu | ws/011-infra-opentofu-host session | done | Owner reviewed and set the shape: nothing applied from this branch; the apply happens from main after 01 merges, so the host installs from the merged flake. Changes since the progress line: coolify_count defaults to 0 so the control-plane VM waits for wave 3 (I-23); the installer reaches hosts through the edge, never a temporary public IP, documented and decided (I-24); the join token comes from `hostdev init` (I-17), not repose-admin, so workstream 05 is not on the path to a registered host; edge_operator_ssh_port is a root variable and must be 22 for the first apply until 06 moves the edge's sshd. Blob soft delete enabled on reposetfstate3912 (30 days, blobs and containers; versioning was already on) - the only change made to real infrastructure. `tofu plan` on prod: 28 to add, 0 to change, 0 to destroy, no errors or warnings; make check clean. Branch is ready to merge.
