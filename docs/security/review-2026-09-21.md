# Security review, 2026-09-21 (workstream 14, final: `main` as deployed)

What was reviewed: not the tree but the machines. `main` at `954ce39`
(api image `74d45e3` on the control VM, host-01 on `hostd-d3568d4`, the
edge on gateway `65d336e`), read on 2026-09-21 between 01:56Z and 02:30Z
from the dev box (`20.102.97.100`, an operator address) with read-only
commands only: the edge over its operator sshd (2222), host-01 through
the edge on its WireGuard address, the control VM over its operator sshd,
Postgres and `repose-admin` through `docker exec`, and the public
surfaces from outside. Nothing was changed on any machine; the two code
fixes below reach the machines when the branch merges (api and web roll
on every push to `main`, I-112) and at the next host switch (hostd).

Method: every boundary row of `docs/workstreams/14-security.md` §5 and
every open finding of [review-2026-09-20.md](review-2026-09-20.md) was
checked against what the running system says, plus the audit log, the
operator path and the never-log list against the live journals. Loki
could not be checked: no Loki is recorded (`repose-admin edge loki`:
"no Loki recorded; hosts ship nothing") and `fluent-bit.service` on
host-01 is skipped by its condition, so nothing has ever shipped.
Severity as in the 2026-09-20 review.

## At a glance

Closed tonight, all 2026-09-21 (merged to `main` by the conductor as
each batch landed: 02:28Z, 02:30Z, 02:36Z, 02:39Z, 02:43Z):

| # | What | How |
|---|---|---|
| M5-1 | `/metrics` on the api's public listener (High) | I-136; 404 in production since 02:30Z |
| M-1 | no Host CA on hosts | I-139; host-01 at the next switch plus the runbook step |
| M-2 | edge sshd defaults | verified closed on the live edge |
| M-4 | hostdev's M1 state on the edge | removed from the edge and host-01 at 02:20Z |
| M-5 | second `wg0.conf` writer in hostd | I-137; host-01 at the next switch |
| L-13, L-7 | operator logins not in `audit_log`, no key id | I-140; host-01 at the next switch, api on merge |
| M5-2 | `repose --version` | fixed; ships at the next tag |
| M5-7 | legal pages client-rendered | prerendered; verify after the web roll |
| M5-9 | `--name` projects fail at sync | I-138 |
| M5-3 | M1-era `argv` in host-01's journal | accepted (owner's own guests, bounded by `SystemMaxUse=4G`) |

Open, with owner and the date it waits for:

| # | What | Owner | Date |
|---|---|---|---|
| host switch | I-137, I-139, I-140 reach host-01 only at a `nixos-rebuild switch`; the runbook's `host_ca_pub` step goes with it | conductor | after m3's kernel row, 2026-09-21 |
| M5-6 | `bootstrap.enable` and its bootstrap key stay on host-01; "only with a certificate" (14 §9) | 01, 11 | once operators hold certificates (after the switch) |
| L-13 (half) | a restore through the user route writes no `audit_log` row | 05 | next 05 session |
| M5-4 | api-grpc `9104` metrics plain HTTP on the whole VNet | conductor (Coolify mapping), 10 | with the monitoring peer |
| M5-5 | Traefik `8080` mapped but not listening | owner | Coolify proxy config, no date |
| M5-8 | `b1a5915` history: rewrite or make private | owner | the owner's decision; nothing happens to history tonight |
| Loki | no Loki recorded, Fluent Bit ships nothing; never-log list unchecked against a log store | owner (monitoring peer, `repose-admin edge loki`) | 10 §9 |
| M-3, L-11 and the rest of the 2026-09-20 list | unchanged | as listed there | |

## Fixed on this branch

| # | Sev | Where | Finding |
|---|---|---|---|
| M5-1 | **High** | `internal/api/http/server.go:127-129` (before), Traefik router `PathPrefix(/)` on the `api` app | `GET https://api.repose.herakraft.co/metrics` answered **200 to the internet** with no token: verified from the dev box, from the edge's public address (`20.102.98.254`) and from the control VM at 02:05Z. The user mux mounted `/metrics` beside `/healthz`, and the proxy fronts the user mux on the public name. I-133's allow-list router was not on the live app (its labels are the two default routers only), and even present it would have been the only gate. `docs/ops/OBSERVABILITY.md` promises nothing observability-related is reachable from the internet. Fixed: the mount is gone, `MetricsHandler` on `:9103` is the only metrics endpoint (DECISIONS I-136), `TestMetricsIsNotOnTheUserListener`. Verified after the merge: `curl -s -o /dev/null -w '%{http_code}' https://api.repose.herakraft.co/metrics` is 404 from this box at 02:47Z (the conductor saw it turn at 02:30Z); it becomes 403 once I-133's router is pasted. |
| M-5 | Medium | `internal/hostd/register/register.go:172,270` (before) | Confirmed on host-01: `/var/lib/repose/hostd/wg0.conf` (0600 root, written at registration 2026-09-20 18:15Z) next to the `host.json` the host actually renders `/run/repose/wg0.conf` from. Two writers of one tunnel and a second copy of the private key on the persistent disk. Fixed: `writeWG` removed, `--no-wg` accepted and ignored for one release (DECISIONS I-137). The stale file on host-01 is inert; the fix reaches host-01 at the next host switch, which the conductor schedules once m3's kernel row is done (2026-09-21); delete the stale file at that switch. |
| M5-7 | Low | `apps/web/src/routes/{privacy,terms}/+page.svelte`, `+layout.ts` (`ssr = false`) | `curl https://repose.herakraft.co/privacy` returned a 1,162-byte shell with neither required passage: the pages rendered client-side, so a crawler, a curl or a browser with scripts off read nothing. Fixed: `+page.ts` on the two routes sets `ssr = true; prerender = true`; the build writes `build/prerendered/{privacy,terms}.html` and both passages grep in them (11,089 and 7,764 bytes). Verified after the web roll at 02:47Z: `curl -s https://repose.herakraft.co/privacy \| tr -s "[:space:]" " " \| grep -c "We sample the processes"` is 1, and the terms phrase likewise. |
| M5-9 | Low | `internal/cli` sync (`git fetch origin` in the guest) | `repose run --name repose-m5-frag --size small --no-attach` in a directory with no git remote (the case `--name` exists for, `cli-config.md`) created and booted the guest in 30 s and then failed at sync: `ssh cd ~/repose-m5-frag && git fetch origin: exit status 128: fatal: 'origin' does not appear to be a git repository`, exit 1. guestd sets `origin` from the project's remote (I-107) and there is none, so the fetch can never succeed; Fixed (DECISIONS I-138): with `remote_url` empty the sync skips the fetch and checkout, sends the tracked files as a tar, adds and commits them in the guest so the tree is clean for the next run, and prints `Synced the whole tree (no git remote): …`; `TestSyncNoRemoteSendsTheWholeTree`. Found by this session at 02:28Z; the same command succeeded on repose-m5-frag with the fixed CLI afterwards. |
| M-1 | Medium | `proto/repose/hostd/v1/hostd.proto` `RegisterResponse` (no `host_ca_pub` before), `internal/api/hostmgr/hostmgr.go` `Register`/`Rotate`, `internal/hostd/register/register.go` `write`, `internal/hostd/app/app.go` `rotationLoop`, `internal/hostdev/server.go` | Still as the 2026-09-20 review found it on host-01: `/run/repose/host_ca.pub` 0 bytes, `sshd -T` `TrustedUserCAKeys /run/repose/host_ca.pub`, 750 `Accepted publickey` in 24 h all by the bootstrap key in `/etc/ssh/authorized_keys.d/root` (2 lines), `hostd audit-login` recording `pam_type` and `user_present` only. Fixed in code (DECISIONS I-139): `RegisterResponse.host_ca_pub = 8` from `ca.HostCAPub()` on Register and Rotate, hostdev sends its SSH CA, hostd writes it to `host.json` (empty keeps the old value) and restarts `repose-host-net` after a rotate that changed it or `loki_url`; `TestRegisterSessionSendSweep` and `TestRegisterThenRotate` assert the line. Reaches host-01 at the switch the conductor schedules after m3's kernel row, plus the runbook's by-hand `host.json` step ("Operator certificate refused by a host") since a registered host only sees the field at its first rotate (about 2026-10-15). Not closed by this: the bootstrap key stays while `repose.host.bootstrap.enable` is on (I-92), so 14 §9's "only with a certificate" waits on 01/11 (M5-6). |
| L-13, L-7 | Low | `internal/hostd/app/app.go` `AuditLogin`, `internal/hostd/control`, `internal/api/events/events.go` `OnEvent` | Confirmed open on production: `audit_log` had 22 actions in 48 h and no `operator_login`, while host-01's journal had 750 accepted logins in 24 h, each a `hostd audit-login` line with `pam_type` and `user_present` only. Fixed (DECISIONS I-140): the hook parses `SSH_AUTH_INFO_0` for the certificate's key id and serial or a plain key's fingerprint (never a body, `TestParseAuthInfo`), hands them to the daemon over the control socket (`TestOperatorLoginRoundTrip`), the daemon emits `operator_login` on the stream, and the api writes one `audit_log` row per host event id (`TestOperatorLoginWritesOneAuditRow`). Reaches host-01 at the switch carrying I-137 and I-139; the api side rolls on merge. A restore through the user route still writes no row (the other half of L-13, 05). |
| M5-2 | Low | `internal/cli/cli.go:47` | `repose --version` answered `unknown flag: --version` (exit 1) while `docs/CHECKLIST.md` "Release (M5)" names that exact invocation. Cobra's `Version` set with a template identical to `repose version`. Ships at the next tag; v0.1.4 as released has only the subcommand. |

## Open, by severity

| # | Sev | Where | Finding | Owner |
|---|---|---|---|---|
| M-4 | Medium | edge `/var/lib/repose-hostdev/`, host-01 `/var/lib/repose/hostd/m1-hostdev/` | **Closed 2026-09-21 02:20Z** (conductor's go-ahead, no copies kept). Before, on the edge: `ca.key`, `ca.pem`, `logs/`, `server.key`, `server.pem`, `ssh_ca`, `ssh_ca.pub`, `state.json` (127,331 B, 2026-09-20 17:25Z: the M1 guests' named secrets and sshd keys in plaintext, the 2026-09-20 review's M-4); `hostdev` inactive and its unit `not-found` (the transient unit is gone, nothing can recreate the directory). After: `ls: cannot access '/var/lib/repose-hostdev'`. On host-01, `m1-hostdev/` held the M1 `cert.pem`, `key.pem`, `host.json` and two `state.db.m1-removed*` copies; after: only the M2 identity (`cert.pem`, `key.pem`, `host.json`, `state.db`, and the stale `wg0.conf` of M-5, left for the next visit). | closed |
| M5-3 | Low | host-01 `journalctl -u hostd`, 2026-09-20 03:56Z to 06:20Z | 14 `exec_audit` lines from the M1 hostd carry `argv` (three of them with paths under `/home/dev/`), the field I-53 removed: the deployed hostd logs `argv_len` only (20 audited execs since the 18:15Z switch, none with `argv`). The residue is the owner's own M1 guests, shipped nowhere (no Loki), and the journal keeps it for the host's life. Left in place (conductor, 2026-09-21): the owner's own guests, shipped nowhere. What bounds it: host-01's `journald.conf` sets `SystemMaxUse=4G` and no `MaxRetentionSec`, with 280 MB in use, so the lines age out by volume rather than by time; a `journalctl --rotate && journalctl --vacuum-time=1d` is the cut if that ever matters. | closed (accepted) |
| M5-4 | Low | control VM `ss -tlnp`: `0.0.0.0:8443`, `:8444`, `:9104` published by the `api-grpc` container; from the edge (`10.200.x`) all three OPEN | The gRPC listener (8443, mTLS, hosts dial it: I-92) and `/internal` (8444, client certificate required: a bare TLS connection is refused) are as designed. `9104` is the api-grpc metrics registry over plain HTTP, reachable from every VNet member (edge, hosts; guests are cut off by the host's private-range drop, verified in `guest_fwd`). Wider than "over WireGuard" in `docs/ops/OBSERVABILITY.md`; the audience is trusted machines. Bind the published port to the WireGuard address (`10.255.255.1:9104:9103` in Coolify's mapping) or leave it and say so. | conductor (Coolify field), 10 |
| M5-5 | Low | control VM: `coolify-proxy` publishes `0.0.0.0:8080` (docker-proxy bound); nothing answers (`000` from the VM and from the edge) | Traefik's API entry point is mapped but not listening. Harmless today; the NSG has no 8080 rule and the postcondition of I-71 forbids one. Hygiene: the mapping invites the day someone enables the dashboard. | owner (Coolify proxy config) |
| M5-6 | Low | host-01 `nft list ruleset` `input`: `iifname "eth0" tcp dport 22 accept`; `sshd -T`: `ListenAddress 10.255.0.2:22` | `repose.host.bootstrap.enable` stays on for host-01 (I-92) so the token delivery path exists; sshd itself binds the tunnel address only, so the rule admits packets to a closed port. Inert, and the NSG for the hosts subnet is empty inbound. Turn bootstrap off when M-1 lands and operator access is by certificate. | 01, 11 |
| M5-8 | Info | `git show b1a5915 --stat`: `.env` with `ANTHROPIC_API_KEY`; `gh repo view`: `PUBLIC`; the value appears in two commits of history, both reachable from `origin/main` | Rotated 2026-09-17; the history is not rewritten and the repository is public. The checklist row is the owner's decision (rewrite with `git filter-repo` and force-push, which invalidates every clone and worktree, or make the repository private before it is shared). Recorded here so the decision has a place to land. | owner |

## Verified as deployed

Each row of the boundary table, with what the machines said.

| Row | Evidence (host-01 unless said) |
|---|---|
| Guest A cannot reach guest B | `bridge repose forward` policy drop, counter 0; `input` admits ARP and IPv4 only from `@guests` (5 tuples matching the 5 running guests' MAC, address and tap; 63 frames dropped); `bridge -d link`: every `tap-*` `isolated on learning off flood off`; `bridge fdb show br br-guests`: 10 static entries; `inet repose guest_fwd`: `ip daddr 10.64.0.0/12 drop` (15 packets). |
| Guest cannot reach host | `input`: `iifname "br-guests" jump guest_in` before the established rule; `guest_in`: `ct direction reply ct state established,related accept`, echo `limit rate 5/second`, drop (150 packets). Listeners on the host: sshd, node_exporter, hostd on `10.255.0.2` only, resolver on loopback. |
| Guest cannot reach IMDS, the wire server, the mesh, private ranges | `guest_fwd`: drop `{168.63.129.16, 169.254.169.254}` (30 packets), drop `10.64.0.0/12`, drop the five private ranges (24 packets); `10.255.0.1` admitted on 8443 and 6081 only; egress out of `eth0` accepted and masqueraded. |
| Guest cannot write the store | `findmnt /run/repose/store-export`: `ro,nosuid,nodev`, propagation `private` (I-61); `.links`: `tmpfs ro,nosuid,nodev,noexec,size=4k,mode=555`; virtiofsd runs as `virtiofsd`, `--sandbox namespace --shared-dir /run/repose/store-export --no-announce-submounts --socket-group hostd`, `MemoryMax=1G`, socket dir `0750 virtiofsd:hostd`. |
| A guest escape does not land as root | all five `guest@<id>` units: `User=hostd`, `NoNewPrivileges=yes`, `CapabilityBoundingSet=` (empty), `ProtectSystem=strict`, `ProtectHome=yes`, `DevicePolicy=closed`, `DeviceAllow` exactly `/dev/kvm`, `/dev/net/tun` and the unit's own `/dev/vg-guests/g-<id>`, `RestrictAddressFamilies=AF_UNIX AF_VSOCK`, `TemporaryFileSystem=/var/lib/repose/guests`, `BindPaths=<own dir>`, `Slice=guests.slice`, `MemoryMax` class+512M, `CPUQuota` vcpus×100%; `ps -o user=` of every MainPID: `hostd`; `--seccomp true` on every argv; guest dirs `1770 root:hostd`, `ch.args` 0640 root, `guest.json` 0600 root, `console.log` 0640 root; `/dev/mapper/vg--guests-g--*` present per guest. |
| Restricted evaluation | host `nix.conf`: `sandbox = true`, `sandbox-fallback = false`, `trusted-users = root`, `allowed-users = nixbuild root`, `max-jobs 2`, `cores 8`; `/var/lib/repose/builds` 0711. |
| mTLS per host | `hosts`: host-01 `ready`, `wg_ip 10.255.0.2`, 5 running; `/var/lib/repose/hostd/{cert,key,host.json}` 0600 in a 0700 dir; no `join-token` left in `/run/repose`. |
| Operator access | edge `sshd -T` (2222): `PasswordAuthentication no`, `KbdInteractiveAuthentication no`, `PermitRootLogin prohibit-password`, `LogLevel VERBOSE`, `AllowUsers root`; nft admits 2222 from `10.255.0.0/16` and the operator `/32` only, final drop counter 0 (the NSG stops the rest first); the 1,597 password and invalid-user lines of the last 24 h all predate the 23:36Z switch, and the 3 auth lines since are accepted operator keys. **The 2026-09-20 review's M-2 is closed on the live edge.** host-01 `sshd -T`: same settings on `10.255.0.2:22`; 750 accepted operator logins in 24 h, every one from `10.255.0.1`, each with a `hostd audit-login` journal line. |
| Never-log list, live journals | host-01 `hostd` (1,515 lines, 24 h): no `email`, `remote_url`, `handle`, `Bearer`, `PRIVATE KEY`, `secret`, `value`, `cmdline` or `env` field; `argv` only in the 14 M1-era lines of M5-3; events are ids, states, kinds, counts, durations. Edge `gateway` (1,288 lines): `auth_fail` carries `reason` and `source_prefix` as a `/24`, never an address; `session_open`/`session_close` carry project ids; no email-shaped string. `/run/repose/host.env` holds ids and addresses only. |
| Audit coverage | `audit_log`, 48 h: `exec` 22, `cert_issue` 20, `project_destroy` 10, `base_publish` 8, `secret_put` 5, `cert_revoke` 4, `host_smoke` 4, `project_create` 4, `secret_delete` 3, `user_exempt` 2, and one each of `project_stop`, `project_snapshot`, `host_add`, `ca_sign_client`, `edge_init`, `user_rename`, `project_start`, `host_register`, `base_rollback`, `billing_enforce`, `ca_sign_host`, `ca_sign_server`. The 22 `exec` rows match the api-driven `exec_audit` lines on the host (`audit_id`, `argv_len`, no argv). Missing producers: L-13. |
| Public surface | edge `20.102.98.254`: TCP 22 (gateway, `*:22`), 2222 (operator sshd), nothing else of 19 ports probed; 443 answers only the preview stub over wg (closed from outside tonight); 51820/udp is WireGuard (two peers: the control VM `10.255.255.1/32`, host-01 `10.255.0.2/32, 10.64.0.0/22`; forward chain is established-only, so hosts cannot reach each other through the hub, L-8). Control VM `20.121.138.150`: 22, 80, 443 only. api over the public name: `/v1/me` without a token is the `unauthenticated` envelope with `X-Request-Id` and the I-79 CORS headers; `/internal/*` and `/v1/internal/*` are 404 on the public app (`/internal` lives on 8444 behind the client certificate, I-42; a bare TLS connection to it is refused); `/healthz` 200. No HSTS header (Traefik's default; low). |
| hostd for host X cannot act on host Y | one host; unchanged. |
| Certificates, 404-not-403, secrets values, hook spoofing, console contents, limits | closed by the M3 session's `test/isolation` run on host-01 (14 §9), not re-run tonight. |

## What this review could not do

- **Loki.** No Loki URL is recorded and Fluent Bit ships nothing, so the
  never-log list was checked against journald on the machines and not
  against a log store. Until the owner's monitoring peer exists (10 §9)
  the console-log row rests on the host-side test only.
- **A second host** (hostd X versus host Y, snapshot restore across
  hosts, host loss) and **a second human** (M5 steps 2 and 3): scheduled
  by the conductor, not this session.
- **The guests' insides** (serial getty on the console, M-6; `dev` in
  `kvm`, L-6; secrets tmpfs modes) were not re-checked tonight; the M3
  session's `secrets.sh` and the isolation suite cover the modes, and
  the base's VM tests cover the units.

## Changes made to the machines

Two, both announced to and approved by the conductor first, both
recorded above: the removal of hostdev's M1 state from the edge and of
the M1 identity directory from host-01 (M-4, 02:19:59Z and 02:20:01Z).
Nothing else was written on any machine.
