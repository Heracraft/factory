# Security review, 2026-09-20 (workstream 14)

What was reviewed: everything merged into `main` at `43efc53` plus this
branch: `nix/hosts`, `nix/guest`, `nix/edge`, `nix/overlay`, `cmd/hostd`
and `internal/hostd`, `cmd/hostdev` and `internal/hostdev`, `cmd/guestd`
and `internal/guestd`, `cmd/repose-hook`, `proto/`, `infra/azure`. The api,
gateway, CLI, dashboard, billing and observability packages are stubs
(`cmd/api`, `cmd/gateway`, `cmd/repose`, `cmd/repose-admin` print "not
implemented"), so every claim that rests on them is listed under "Not
verifiable yet" rather than passed.

Method: each boundary row of `docs/workstreams/14-security.md` §5 was
traced from `docs/SECURITY.md` to the file that implements it, and the
implementation was compared with the interface doc that binds it. Two
claims were reproduced on the dev box (virtiofsd's sandbox modes; the
nftables ruleset by reading the VM test). Line numbers are for this
branch; "before" numbers are for `43efc53`.

Severity: **High** breaks a tenant boundary or the host, or blocks every
guest; **Medium** weakens a boundary, a promise in the docs, or an audit
path; **Low** is defence in depth, hygiene or a doc mismatch.

## Fixed on this branch

| # | Sev | Where | Finding |
|---|---|---|---|
| H-1 | High | `internal/hostd/net/net.go` (before: 77-82, 106-120; golden `testdata/create.golden`), consumer `internal/hostd/guest/create.go:231-239` | hostd attached taps with a bare `ip tuntap add … mode tap` and `ip link set master`, without `isolated on learning off flood off`, without the static FDB entry, and registered the guest in `inet repose guests { ip . tap }`, a set no host declares (I-18 put admission in `bridge repose guests` with type `ether_addr . ipv4_addr . ifname`). On a host, `nft add element` fails and every `CreateGuest` ends at step 6. Had the set been declared to make it pass, `learning off` missing would let a guest send frames with another guest's source MAC: the bridge learns before the nftables input hook drops the frame, and from then on the other guest's inbound traffic (the gateway's SSH, its DNS answers) is delivered to the attacker's tap. Fixed: the `Net` interface carries the MAC and the golden test is the command list from `host-conventions.md` "Network" verbatim. Needs the first host to confirm (`bridge link show`, `bridge fdb show br br-guests`, `nft list set bridge repose guests`). |
| H-2 | High | `internal/hostd/guest/create.go` (before: 257-262), `internal/hostd/systemd/systemd.go:245`, `nix/hosts/hostd.nix` (before: 278-293) | Cloud Hypervisor ran as root: `docs/SECURITY.md` accepted a KVM escape landing in the Azure VM, and as built it landed as root, which is every tenant on the host plus the host's mTLS identity and Blob credential. Fixed on `ws/14-security-followup` (DECISIONS I-51): `guest@<id>` carries `User=hostd` and the sandbox pinned by `internal/hostd/guest/testdata/unit.golden` (`NoNewPrivileges`, empty `CapabilityBoundingSet`, `ProtectSystem=strict`, `DevicePolicy=closed` with `/dev/kvm`, `/dev/net/tun` and the guest's own volume, `TemporaryFileSystem`+`BindPaths` so only its own guest directory is visible, `RestrictAddressFamilies=AF_UNIX AF_VSOCK`); `--seccomp true` is on the argv; `hostd` is in `kvm`; a udev rule makes `vg-guests/g-*` group `hostd`; guest directories are `1770 root:hostd` with a `virtiofsd/` socket directory (`--socket-group hostd`). Tests: `TestGuestUnitPropsGolden`, `TestPrepareGuestDirLayout`, `TestCreateReachesRunningWithEverythingWired`; VM tests `host-storage` (LV node `root:hostd:660`, `DeviceAllow` on the LV symlink) and `host-services` (a unit with the property list runs as `hostd`, opens the tap, binds a socket in its directory, sees no other guest, is refused `AF_INET` and `/dev/vhost-vsock`); `test/isolation` `TestHypervisorRunsAsHostdUser` for the first host. |
| H-3 | High | `internal/hostd/virtiofs/virtiofs.go` (before: 31) | virtiofsd started as user `virtiofsd` with `--sandbox chroot`. virtiofsd 1.14.0 refuses: `Error entering sandbox: sandbox mode 'chroot' can only be used by root (Use '--sandbox namespace' instead)` (reproduced, exit 1). Every create would fail at step 8; the tempting fix, dropping `User=`, would have run a root virtiofsd with the whole store in front of tenants. Fixed to `namespace`, which `03-hostd.md` §5.5 and the runner's `bin/virtiofsd` already said. DECISIONS I-48; `host-conventions.md` and `01-host-nixos.md` corrected. Verify on the host: `systemctl status virtiofsd@<id>` active, `ls /nix/store` inside the guest. |

## Open, by severity

| # | Sev | Where | Finding | Owner |
|---|---|---|---|---|
| M-1 | Medium | `proto/repose/hostd/v1/hostd.proto:37-44` (`RegisterResponse`), `internal/hostd/register/register.go:585-599`, `internal/hostdev/server.go:136,162`, read by `nix/hosts/network.nix:278` and trusted by `nix/hosts/ssh.nix:109` | No Host CA ever reaches a host. `RegisterResponse` has no `host_ca_pub`, `register.go` never fills `HostJSON.HostCAPub`, hostdev sends none, so `/run/repose/host_ca.pub` is always empty and the certificate-only operator access of `host-conventions.md` "Operator access" and `14-security.md` §5 cannot happen. The only operator path is the bootstrap key on the provider NIC (`nix/hosts/host-01.nix:393-396`, `nix/hosts/nftables.nix:93-96`): a static key, never rotated, with no certificate serial in the audit line. Proposal: `RegisterResponse.host_ca_pub = 7` (also returned by `Rotate` so a CA rotation reaches hosts), hostdev sends its SSH CA public key, the api sends the Host CA. Operator certificates need principal `root`. | 03 (message shapes), 05 |
| M-2 | Medium | `nix/edge/default.nix:250` (`services.openssh.enable = true`, nothing else), `infra/azure/modules/network/main.tf:134-151` (22 open from the internet) | The edge's sshd is on a public port with NixOS defaults, which are `PasswordAuthentication yes` and `KbdInteractiveAuthentication yes`. Root has no password there, so no login can succeed today, but the workstream's checklist item "a password attempt is logged" cannot be closed with an accidental configuration, and the box is a brute-force target from the first apply. Set `PasswordAuthentication = false`, `KbdInteractiveAuthentication = false`, `PermitRootLogin = "prohibit-password"`, `LogLevel = "VERBOSE"`, and install the same `hostd audit-login` PAM hook the hosts have (or the gateway's equivalent) before 06 moves operator sshd to 2222. | 06 (edge config), 11 (NSG) |
| M-3 | Medium | `infra/azure/modules/storage/main.tf:107-121` (`Storage Blob Data Contributor` for the one host identity on the whole container), `nix/hosts/host-01.nix:401-405` | Every host authenticates to Blob with the same identity and can read, overwrite and delete every tenant's snapshot on every host. With H-2 open, a guest escape on one host reaches every project's disk image in the fleet. Mitigations, in order of cost: one container per host with a per-host identity scoped to it (the api restores across hosts with its own identity); or the api hands hostd a short-lived SAS per upload and per download so hosts hold no standing Blob credential. Recorded in `docs/SECURITY.md` "Not mitigated" until one lands. | 11, 03, 05 |
| M-4 | Medium | `internal/hostdev/statefile.go:42-45` (`HostKey`, `Secrets`, `Fragment` persisted), `internal/hostdev/server.go:405-409` (`/status` returns them), `internal/hostdev/cli.go:395-422` (`--secret K=V` on the command line) | hostdev keeps named-secret values and guests' sshd private keys in plaintext in `state.json` on the edge's disk and returns them to any process that can open its control socket; secrets typed as `--secret K=V` are visible in `ps` and shell history on the edge. This is a fourth home for secrets (AGENTS.md "Secrets have three homes"). Acceptable for the owner-only M1 period (state dir 0700, file 0600, the edge is the operator's own VM) and must not outlive the api: `hostdev` should refuse to `serve` once `api.repose.herakraft.co` resolves, or be removed from the edge image at M2. Cheap improvements now: read `--secret` values from a file or stdin; strip `Secrets`, `HostKey` and `Fragment` from `/status` and have `start` load them server-side. | 03 (hostdev) |
| M-5 | Medium | `internal/hostd/register/register.go:636-650` (`writeWG`), `internal/hostd/app/app.go:97` (`WGUnit`), `nix/hosts/registration.nix:358` | After registration hostd writes its own `/var/lib/repose/hostd/wg0.conf` and restarts `wg-quick-wg0.service`, while `repose-host-net` renders `/run/repose/wg0.conf` from `host.json` and restarts the same unit (I-18: hostd writes `host.json` only). Two writers of the tunnel config and two restarts race; the WireGuard private key also sits in a second file on the persistent disk. Delete `writeWG` (keep `WGConf` for tests) and make `--no-wg` the only behaviour. Security impact is low (the key is already in `host.json`, 0600); the race is the bug. | 03 |
| M-6 | Medium | `nix/guest/base/boot.nix:359-362`, `nix/guest/microvm.nix:92,210` (`console=ttyS0`), `nix/guest/base/guestd.nix:35-36` (`journal+console`) | The kernel line puts the console on ttyS0, so NixOS starts `serial-getty@ttyS0` and offers a login prompt on the very channel hostd copies into `console.log` and Fluent Bit ships to Loki for 30 days. Both accounts have locked passwords (`users.nix:112,117`), so no login succeeds, but whatever is typed at that prompt (an operator attached to `console.sock`, a mistyped password) becomes a stored log line. The boundary row "console logs do not carry terminal contents" holds for tenants' shells today; keep it structural: `systemd.services."serial-getty@ttyS0".enable = false` in the guest base, so the console is output-only. | 02 |
| M-7 | Medium | `internal/hostd/nixbuild/nixbuild.go:302-306` (every stderr line streamed as `BuildLog`), `docs/features/secrets.md` "Secrets appear in no log … build logs are scanned" | No component redacts secret values from build logs yet. hostd streams Nix's stderr verbatim to the api; the scan-before-storage the secrets feature promises is assigned to the api and 12, neither of which exists. Until it does, a fragment that `echo`es an environment variable puts a secret in `build_logs` for the life of the project. Gate `PUT /secrets` on that redaction landing. | 05, 12 |
| M-8 | Medium | `internal/hostd/guest/snapshot.go:34-39` (`BlobPath` joins `user_id`, `project_id`), `internal/hostd/snapshot/blob.go:54` (`filepath.Join(f.Dir, c.BlobPath)`), `internal/hostd/guest/snapshot.go:132-154` (`Restore` takes `blob_path` as given) | hostd validates `guest_id` (`hexPrefix`), `revision_id` and `base_ref` (`nixbuild.go:144,184`) but not `user_id`, `project_id` or `blob_path`. With the local snapshot backend a `blob_path` of `../../..` reads or writes outside the snapshot directory as root; the Blob backend is not affected. The api is a trusted peer, but every other id is validated and this one is a path. Reject `..`, absolute paths and anything outside `[A-Za-z0-9._-]` per segment. | 03 |
| L-1 | Low | `nix/hosts/nftables.nix:102` | The ICMP echo exception for guests is one global `limit rate 5/second`, not per guest; one guest flooding the host's address starves every other guest's debugging pings. `meter` by `ip saddr` if it matters. | 01 |
| L-2 | Low | `docs/workstreams/14-security.md` §5 row "Guest cannot reach host" | Said `ping .1` fails; I-18 and `host-conventions.md` keep a deliberate 5/s echo exception. Row corrected on this branch to match the contract; the test asserts the rate limit. | 14 (done) |
| L-3 | Low | `internal/hostdev/sshca.go:646-651, 703-751` | hostdev signs guest host certificates and user certificates with one key; `ssh-gateway.md` requires two CAs. Harmless in the stand-in (certificate types differ and sshd checks them), must not be copied into the api. | 05 |
| L-4 | Low | `internal/hostdev/server.go:115` | Join token compared with `!=` rather than `subtle.ConstantTimeCompare`; the token is 32 random bytes, so timing is not exploitable in practice. | 03 |
| L-5 | Low | `internal/guestd/secrets/secrets.go:935,959` | guestd accepts secret names up to 128 bytes matching `[A-Za-z_][A-Za-z0-9_]*`; `api.md` promises `[A-Z][A-Z0-9_]{0,63}`. The api must enforce the narrower shape; guestd's leniency is fine as a second line. | 05 |
| L-6 | Low | `nix/guest/base/users.nix:108` | `dev` is in group `kvm` inside the guest. There is no `/dev/kvm` in a Cloud Hypervisor guest; drop it so the group list says only what is used. | 02 |
| L-7 | Low | `internal/hostd/app/app.go:406-413`, `nix/hosts/ssh.nix:125-130` | `hostd audit-login` records `pam_type` and `user_present` only: no certificate key id or serial, so an operator login cannot be tied to the certificate that made it (sshd's VERBOSE line has `ID <key_id> (serial N)`). The PAM hook can read `SSH_AUTH_INFO_0` for the key id (never the certificate body), or the api's audit ingestion (05) can pair the two journal lines. `pam_exec` is `optional` by design, so a broken hook loses rows silently; 10 should alert when sshd accepts a login and no `operator_login` event follows. | 03, 05, 10 |
| L-8 | Low | `nix/hosts/nftables.nix:85`, `nix/hosts/network.nix:264` (`AllowedIPs 10.255.0.0/16`) | Hosts accept sshd and the exporters from any WireGuard peer, and route the whole mesh through the tunnel, so host-to-host reachability is decided by the edge's forward policy alone. `06-gateway-edge.md` §5.1 says forward is wg0→wg0 for the gateway only; make that a checklist item with a test (host A cannot reach host B's 22, 9100, 9101). | 06 |
| L-9 | Low | `internal/hostd/guest/harness_test.go`, `internal/hostd/vsockclient/client_test.go` | Not gofmt-clean; `docs/CHECKLIST.md` requires it. | 03 |
| L-10 | Low | `nix/hosts/kernel.nix:435` | `net.ipv4.ip_nonlocal_bind=1` host-wide so sshd and node_exporter can bind the WireGuard address before the tunnel is up. Root-only host, so harmless; `systemd.services.<unit>.after = wg-quick-wg0` or `FreeBind` in the socket units would remove the global switch. | 01 |
| L-11 | Low | `internal/hostd/guest/unit.go` (`User=hostd` for every guest), `internal/hostd/net/net.go:94` (`ip tuntap add ... user hostd`) | After I-51 every guest's hypervisor is the same uid. `DeviceAllow` limits each unit to its own volume and the private guests directory hides every other guest's sockets, but every tap is owned by `hostd`, so an escaped hypervisor could `TUNSETIFF` another guest's tap and read or inject that guest's frames (the bridge still admits only the registered MAC and IP per tap, so it cannot impersonate the host or a third guest). One system user per guest, with the tap and the volume owned by it, closes it; `DynamicUser=` does not fit because the owner must exist before the unit does. Recorded in `docs/SECURITY.md` "Not mitigated". | 03 |

## Verified as implemented

Each row of the boundary table, with what was read. "By construction"
means the configuration makes it so and the host test confirms it on a
real host.

| Row | Evidence |
|---|---|
| Guest A cannot reach guest B | `nix/hosts/nftables.nix:140-158`: `bridge repose forward` policy drop with no rules; `input` admits ARP and IPv4 only from `(mac, ip, tap)` in `guests`. Taps attached `isolated on learning off flood off` with a static FDB entry (H-1 fix). Routed cross-guest traffic dropped at `guest_fwd:121`. VM test `nix/hosts/tests/default.nix:168,192` fails a ping between two attached namespaces. |
| Guest cannot reach host | `nftables.nix:80` sends every frame from `br-guests` to `guest_in` before the established rule; `guest_in` (100-104) accepts rate-limited echo and drops the rest. VM test lines 170-173. |
| Guest cannot reach IMDS | `nftables.nix:119` drops `169.254.169.254` and `168.63.129.16`; `:125` drops all of `169.254.0.0/16`. VM test line 163. |
| Guest cannot reach other hosts' guest ranges | `nftables.nix:121` drops `10.64.0.0/12`; `:125` drops every private range. VM test line 169. |
| Guest cannot write the store | `nix/hosts/virt.nix:27-31`: ro,nosuid,nodev bind of the store at `/run/repose/store-export` with an empty read-only tmpfs over `.links`; virtiofsd runs as `virtiofsd:virtiofsd` in a namespace sandbox (H-3) sharing the export (`internal/hostd/guest/create.go:245`); the guest mounts the tag under an overlay whose upper dir is its own volume (`nix/guest/base/store.nix`, `nix/guest/microvm.nix:69-79`). |
| Guest cannot see host block devices | `internal/hostd/ch/ch.go:57-73`: one `--disk`, one `--net`, one `--fs`, one `--vsock`, one serial. Since I-51 the hypervisor process itself can open no block device but its own volume (`DevicePolicy=closed`, `DeviceAllow`; VM test `host-storage`). |
| Guest cannot escape memory or CPU limits | `create.go:257-261`: `MemoryMax=<class+512M>`, `CPUQuota=<vcpus*100>%`, `Slice=guests.slice`; `nix/hosts/hostd.nix:209-232` caps the slice at RAM minus the reserve; virtiofsd `MemoryMax=1G` (`virtiofs.go`); builds in a scope with `CPUQuota` and `MemoryMax=16G` (`nixbuild.go:234-238`); tc HTB 200 Mbit per tap (`net.go`). Ten-percent claim needs the host test. |
| Hook socket cannot spoof another project | `internal/guestd/hooks/hooks.go:699-705`: the payload has no project or guest field; unknown fields are ignored, unknown agents rejected with `hook_bad_payload` (`:895`); hostd stamps `GuestId` from its own monitor (`create.go:581`). guestd logs kind and byte count, never the summary (`hooks.go:851-852`). |
| Process samples carry names, CPU, memory | `proto/repose/hostd/v1/hostd.proto:216` (`ProcSample{comm, cpu_ns_delta, rss_bytes}`); `internal/guestd/sample/proc.go` reads `stat` and `status` only; `internal/guestd/strace_test.go` runs guestd under `strace -e openat` and fails on any `cmdline` open and on any `environ` open other than the documented `TMUX_PANE` lookup (`hooks.go:879-891`). `test/isolation/policy_test.go` pins the field set. |
| Secrets never leave ciphertext and the guest tmpfs | hostd keeps them in memory only (`internal/hostd/guest/manager.go:551-586`), never in bbolt or `guest.json` (`internal/hostd/state/state.go:39-65`), delivers over vsock; guestd writes `0400 dev` files in a `0700 dev` directory and `secrets.env` with shell quoting (`internal/guestd/secrets/secrets.go:1015-1030, 1086-1112`), logs counts only (`:1044`). Reserved sshd names cannot be set through the secrets path (`manager.go:539-549`). Host disk holds nothing (`host-conventions.md`, confirmed). |
| Claude credentials never copied | `rg credentials.json cmd internal` returns nothing today (no CLI yet); `nix/overlay/agents/agent-setup.nix` touches only `settings.json`, `.claude.json`, `config.toml` and the opencode plugin. Pinned by `TestClaudeCredentialsAppearOnlyAsAnExclusion`. |
| Thin volumes do not leak freed blocks between tenants | `nix/hosts/disko-layout.nix:119-123`: pool created with `--zero y`; a partial first write to a chunk cannot expose the previous owner's bytes. VM test line 240 prints `zero`. |
| mTLS per host | `internal/hostd/register/register.go:491` TLS 1.3 with the host certificate; hostdev verifies the chain and pins the CN to the one registered host (`internal/hostdev/server.go:98-108, 167-175`). |
| Restricted evaluation | `internal/hostd/nixbuild/nixbuild.go:199-208`: `restrict-eval`, `allow-import-from-derivation false`, `pure-eval`, `eval-cache false`, fragment passed as a path input; sandboxed build in a bounded scope; `revision_id` and `base_ref` validated against path traversal. Host nix: `trusted-users` and `allowed-users` root only (`nix/hosts/gc.nix:470-471`). |
| Never-log list in hostd and guestd | Every `log.*` call in `internal/hostd` and `internal/guestd` was read: fields are ids, states, kinds, counts, durations, error codes and `err.Error()` of local commands. The one `argv` field is the audited operator `Exec` (`internal/hostd/guest/misc.go:74`), as the contract says. Fluent Bit keeps only `MESSAGE`, `PRIORITY`, `component`, `unit`, `_PID`, `SYSLOG_IDENTIFIER` from the journal (`nix/hosts/observability.nix:114-117`), which drops `_CMDLINE`. |

## Not verifiable yet (producer is a stub)

| Row | Waits on |
|---|---|
| A's certificate cannot open B at the gateway; revoked certificate rejected within 30 s | 06 gateway, 05 `/certs` and `/internal/revoked`. Guest-side sshd half is configured (`nix/guest/base/ssh.nix:37-45`: `AuthorizedPrincipalsFile`, `TrustedUserCAKeys`, `authorizedKeysFiles none`) and tested by 02's VM test with a certificate login. |
| Cross-user project lookups are 404 | 05 |
| `GET /secrets` has no values; `audit_log` has no values | 05 |
| hostd for host X cannot act on host Y | 05 (the api's per-stream check); hostdev pins one host. |
| Every audited action writes an `audit_log` row; `repose-admin audit` | 05 |
| Operator access only by certificate | M-1 first, then 05's `operator-cert` |
| Secrets review comments for 05 and 07 | those workstreams |

## Secrets handling review (14 §5)

- **04 guestd** (reviewed, no findings beyond L-5): values land as 0400
  `dev` files on tmpfs; `secrets.env` is 0400 and shell-quoted; reserved
  sshd material goes to `/run/repose` root-owned; validation precedes the
  first write; nothing logs a value; the whole-set semantics make `rm`
  reach a running guest (I-30).
- **03 hostd**: secrets and sshd material stay in memory (`m.secrets`),
  never in bbolt, `guest.json` or `ch.args`; delivered after `Ready` over
  vsock. The gap noted here at review time, H-2 (a root hypervisor), is
  fixed by I-51; what remains is L-11.
- **hostdev**: M-4.
- **05, 07**: not written yet; the review checklist is in
  `docs/SECURITY.md` "Reviewing a workstream".
